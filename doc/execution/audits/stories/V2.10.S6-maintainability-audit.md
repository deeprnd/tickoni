# V2.10.S6 Maintainability Audit

**Story:** V2.10.S6 — CI cutover to fully-qualified recipes
**Audit date:** 2026-08-29
**Auditor:** Repository maintainability review
**Scope:** Story-owned CI command routing and its supporting recipe/checker/documentation surface: `.github/workflows/{build-fd,build-tk,coverage,quality,security,tests-long,tests-short,tests-xlong}.yml`, `.github/actions/setup-public-gh-runner/action.yml`, `.github/actions/build-fd-tk-libs/action.yml`, `justfile`, `contrib/test/check_ci_command_surface.py`, `contrib/test/check-just-recipe-grid.sh`, and the S6 execution documentation. Earlier S2/S4 implementation and unrelated repository reorganization are treated as pre-existing/out of scope except where they are a dependency of this surface.

## Verdict

**Acceptable with explicitly recorded follow-up debt; no unresolved high-severity
maintainability defect was introduced by the S6 cutover.** The qualified recipe
grid and the retained workflow entrypoints are present, the repository-local
checkers pass, and the available quality gate was attempted. Native full CI
validation and the full quality gate are environment-limited on this Linux host
because `zig` is not installed.

The story remains `Backlog`; this audit does not update roadmap status or
acceptance checkboxes.

## Review basis and scope boundary

The review used:

- `doc/execution/contribution/tickoni.md`, especially explicit ownership,
  bounded/direct implementation, deliberate naming, dependency hygiene, and
  test-before-handoff guidance.
- ISO/IEC 25010 maintainability characteristics: modularity, reusability,
  analysability, modifiability, and testability.
- `doc/strategy/roadmap/stories/v2.10-s6.md` and
  `doc/execution/plans/v2.10-s6.md` for the story contract and S6/S7 scope split.
- The live branch source and the S6 implementation history.

The plan defines eight retained command-bearing workflows (`v2.10-s6.md:53-68`),
while the older story text says “7” (`v2.10-s6.md:8,27,46`). The live checker
covers eight workflows plus two composite actions (`contrib/test/check_ci_command_surface.py:12-26`). The eight-workflow plan and live tree are used as the objective current scope.

## Scope and ownership assessment

| Area | Result | Evidence |
|---|---|---|
| Recipe ownership | Pass with follow-up | Workflow build/test/quality/security operations call named `just` recipes (`.github/workflows/build-fd.yml:52-168`, `tests-short.yml:55-595`, `quality.yml:44-50`, `security.yml:40-82`). Recipe bodies own implementation in `justfile`; CI does not invoke Make, Zig, or repository scripts directly in the covered files. |
| Router vs implementation boundary | Pass | Bare local dispatchers remain in `justfile`, while qualified targets are separately addressable. The recipe-grid checker asserts aggregate recipes call bare dispatchers (`contrib/test/check-just-recipe-grid.sh:82-108`). |
| Composite action boundary | Partial; accepted debt | `setup-public-gh-runner` and `build-fd-tk-libs` are thin `just` wrappers (`.github/actions/setup-public-gh-runner/action.yml:51-67`, `build-fd-tk-libs/action.yml:11-15`). Other local actions are not included in the checker inventory; see MAINT-01. |
| Platform source of truth | Pass | The justfile obtains platform values from `contrib/platform.sh` (`justfile:52-67`); qualified workflow names make the CI lane explicit. |
| API and ownership boundaries | Pass | S6 changes CI/build orchestration only. No Tickoni runtime, tile, financial schema, policy, audit, replay, storage, model, adapter, or execution ownership is changed. |
| Naming and documentation | Partial; accepted debt | Qualified names are consistent in most workflow calls, but the story/CI documentation count is stale and some local action/recipe names remain intentionally generic; see MAINT-02. |
| Dependency hygiene | Pass with environment limitation | No new package manifest or third-party runtime dependency was added. The quality invocation uses existing repository tooling; execution could not proceed because `zig` is absent. |
| Duplication and complexity | Pass with bounded debt | Qualified aliases keep platform intent visible and delegate to shared implementations. The checker has a fixed inventory and regex-based YAML parsing; its coverage limitation is recorded as MAINT-01 rather than hidden. |
| Dead code | Pass for S6 surface | Deleted benchmark/book workflows are absent and the checker verifies their absence (`contrib/test/check_ci_command_surface.py:98-105`). Compatibility aliases and local dispatchers remain reachable by design. |

## ISO/IEC 25010 assessment

### Modularity

**Pass with accepted boundary-coverage debt.** CI lane selection is separated
from implementation: workflows select qualified recipes, while `justfile` owns
setup/build/test behavior. Aggregate recipes retain the required bare-dispatcher
boundary. The checker itself does not yet cover every local composite action,
which weakens the enforcement module but does not merge runtime concerns.

### Reusability

**Pass with bounded duplication.** The shared `justfile` recipe bodies and
`contrib/build`/`contrib/setup` helpers are reused by qualified aliases. The
remaining repeated workflow calls are lane declarations, not copied shell
implementations. The thin composite actions centralize GitHub-specific
orchestration without duplicating build logic.

### Analysability

**Pass with documentation/checker debt.** Qualified names expose platform and
compiler intent directly. `check-just-recipe-grid.sh` checks the expected grid,
aggregate boundaries, Linux-only CET placement, and FD/Tickoni test separation.
However, its fixed action inventory and the stale “seven workflows” wording can
mislead maintainers about the complete command surface (MAINT-01 and MAINT-02).

### Modifiability

**Pass with accepted follow-up.** A lane-specific command can normally be
changed at one qualified recipe or workflow call site. Compatibility aliases
remain thin delegates. Extending the checker requires keeping its hard-coded
workflow/action inventory synchronized with the tree; that is the main
cross-cutting maintenance risk and is assigned to the CI owner.

### Testability

**Pass for Linux; cross-platform evidence remains environment-limited.** The
S6-specific checker self-test, live command-surface scan, recipe-grid check,
recipe listing, dry-runs, full quality gate, Linux Zig unit tests, and Linux
integration tests passed. macOS, Windows, and GitHub runner behavior require
CI/native-host evidence and are not claimed here.

## Findings and remediation decisions

### MAINT-01 — Command-surface checker does not cover the full composite-action surface

- **Severity:** Medium
- **Characteristics:** Modularity, analysability, modifiability, testability
- **Affected paths:** `contrib/test/check_ci_command_surface.py:12-26,72-106`; `.github/actions/` (particularly actions outside the two listed in `ACTIONS`); `.github/workflows/tests-xlong.yml:80-87`; `.github/workflows/tests-long.yml:48-50`
- **Objective observation:** The checker hard-codes eight workflows but only two
  composite actions. A repository search found additional local actions and
  command-bearing action/workflow content not represented by the checker. The
  checker therefore passes without proving that every local action is a thin
  `just` wrapper. The same limitation makes future action additions easy to
  miss.
- **Decision:** **Accepted debt for this audit; no CI behavior change made.**
  The current covered workflow surface and S6 checker pass, but enforcement is
  incomplete relative to the plan's exclusive-command-surface goal.
- **Owner:** S8 CI/platform lane owner
- **Follow-up task:** Replace the fixed action allowlist with a deterministic
  inventory of all `.github/actions/*/action.yml` files, or explicitly classify
  each excluded GitHub-native action. Add a regression fixture containing a
  forbidden command in an additional action and require the checker to fail.
  Update the expected count and run the checker in the canonical quality/test
  lane.

### MAINT-02 — Workflow-count and command-surface documentation drift

- **Severity:** Low
- **Characteristics:** Analysability, modifiability, testability
- **Affected paths:** `doc/strategy/roadmap/stories/v2.10-s6.md:8,27,46`;
  `doc/execution/ci.md:38`; `doc/execution/plans/v2.10-s6.md:53-68`;
  `contrib/test/check_ci_command_surface.py:12-26`
- **Objective observation:** The story and CI overview use “7” while the
  execution plan and live workflow inventory contain eight retained workflows:
  `build-fd`, `build-tk`, `coverage`, `quality`, `security`, `tests-long`,
  `tests-short`, and `tests-xlong`. The checker reports `retained workflows: 8`.
- **Decision:** **Accepted documentation debt, not silently normalized during
  an audit.** The execution plan and live checker are the current objective
  sources; changing roadmap language is a documentation-owner decision.
- **Owner:** S8 roadmap/documentation owner
- **Follow-up task:** Reconcile the story and `doc/execution/ci.md` to the
  eight-workflow inventory, add a single source-of-truth inventory or generated
  count, and verify all references with the documentation link checker.

### MAINT-03 — Native cross-platform regression coverage is unavailable on this host

- **Severity:** Low, environment-limited
- **Characteristics:** Testability, analysability
- **Affected paths:** `justfile:623-700`; `contrib/quality/quality.sh:16`;
  all platform-qualified recipes in `justfile`; CI workflows
- **Objective observation:** The Linux host cannot provide native macOS/Windows
  runner evidence. The previously recorded missing-Zig blocker was cleared;
  `just quality-check-all`, the qualified Linux Zig unit lane, and the qualified
  Linux integration lane now execute successfully. Dry-runs alone would validate
  only command rendering, but these completed lanes validate build/test behavior.
- **Decision:** **Linux coverage remediated; cross-platform evidence remains
  environment-limited.** No fabricated macOS/Windows pass is asserted.
- **Owner:** S8 CI/platform lane owner
- **Follow-up task:** **Partially complete** — run `just quality-check-all` and
  the qualified Linux unit and integration lanes on the provisioned host; attach
  successful native macOS/Windows CI results for the retained matrix when
  available.

### MAINT-04 — Branch diff contains a trailing blank-line quality error in CI documentation

- **Severity:** Low
- **Characteristics:** Analysability, modifiability
- **Affected paths:** `doc/execution/ci.md:295-296`
- **Objective observation:** `git diff --check $(git merge-base HEAD main)..HEAD`
  reports `doc/execution/ci.md:295: new blank line at EOF.`
- **Decision:** **Remediated.** The extra EOF blank line was removed and the
  repository whitespace check now passes.
- **Owner:** S8 documentation owner
- **Follow-up task:** **Complete** — remove the extra blank line at EOF, rerun
  `git diff --check`, and include the clean result with the story closure
  evidence.

## Negative checks

- No Tickoni runtime, tile, policy, audit, replay, storage, model, adapter, or
  execution API boundary was modified by the S6-scoped implementation.
- The command-surface checker self-test proves that its synthetic `zig build`
  violation is rejected (`contrib/test/check_ci_command_surface.py:122-151`).
- The live checker reports eight retained workflows, two deleted workflows, and
  `command-surface: PASS`.
- The recipe-grid checker reports `recipe grid OK: 50 qualified recipes`.
- `just --list` succeeded and listed 179 lines.
- Qualified dry-runs rendered successfully for setup, FD unit, Tickoni unit,
  and test-preparation lanes. Dry-run output is not native execution evidence.
- No new dependency manifest, TODO/FIXME marker, or dead compatibility alias was
  introduced in the S6-scoped files.

## Evidence To Attach

Commands were run from `/home/vicgenin/work/git/tickoni` on a Linux host on
2026-08-29. Native macOS/Windows results are not available from this host.

| Command | Result |
|---|---|
| `python3 contrib/test/check_ci_command_surface.py --self-test` | **PASS** — `self-test: PASS`. |
| `python3 contrib/test/check_ci_command_surface.py` | **PASS** — `covered files: 10`; `retained workflows: 8`; `deleted workflows: 2`; `command-surface: PASS`. |
| `bash contrib/test/check-just-recipe-grid.sh` | **PASS** — `recipe grid OK: 50 qualified recipes`. |
| `just --list` | **PASS** — exit 0; 179 output lines. |
| `just --dry-run setup-linux-x86-gcc` | **PASS** — rendered Linux GCC setup and ops commands. |
| `just --dry-run test-unit-fd-linux-x86-gcc` | **PASS** — rendered FD build/test command with Linux-only `-Wl,-z,shstk`. |
| `just --dry-run test-unit-tk-linux-x86` | **PASS** — rendered Zig unit-test command. |
| `just --dry-run test-prep-linux-x86` | **PASS** — rendered resource-limit preparation command. |
| `just quality-check-all` | **PASS** — exit 0; Zig format/build checks, FD lint checks, Tickoni lint checks, and protobuf checks completed. |
| `just test-unit-tk-linux-x86` | **PASS** — exit 0; all 69 tests passed. |
| `just test-integration-tk-linux-x86` | **PASS** — exit 0; all 4 tests passed. |
| `git diff --check $(git merge-base HEAD main)..HEAD` | **PASS** — no whitespace errors; MAINT-04 remediated. |
| `git status --short` after verification | **CLEAN** — quality badge mutation was reverted; no unrelated working-tree changes remain before adding this audit document. |

## Remediation status

No code remediation was performed because this was an audit-only task and the
remaining findings are documentation/checker follow-up debt or cross-platform
host limitations.
MAINT-04 was remediated by removing the trailing blank line and verifying the
branch diff whitespace check. All findings have an explicit status, owner, and
concrete follow-up task. The S6 surface is acceptable for handoff with
MAINT-01 and MAINT-02 carried forward; Linux quality and regression coverage
for MAINT-03 is remediated, while native macOS/Windows evidence must not be
represented as passing results.
