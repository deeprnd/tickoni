# V2.10.S6 — Evidence and Quality Gate

**Story:** `doc/strategy/roadmap/stories/v2.10-s6.md`
**Plan:** `doc/execution/plans/v2.10-s6.md`
**Quality source:** `doc/execution/quality.md`
**Audit date:** 2026-08-29
**Auditor:** Hermes closing gate

## Verdict

**ACCEPTED — quality gate passed with explicit S8 follow-ups.**

The Linux command-surface, recipe-grid, unit, integration, quality, and
security checks pass. macOS and Windows validation is covered by the CI lanes,
not this Linux host. The retained composite-action allowlist mismatch,
external-linter findings, and seven-versus-eight workflow documentation
discrepancy are explicitly deferred to S8 and are not S6 closure blockers.

The quality gate is accepted. The roadmap story status remains `Backlog`; no
roadmap status transition was requested or performed by this gate.

## Scope and reconciliation basis

S6 changes CI/build command routing, qualified `just` recipes, CI composite
actions, command-surface validation, and execution documentation. It does not
change Tickoni runtime tiles, topology, policy, audit/replay semantics,
model/tool/adapter/execution boundaries, CaseOps, storage, or financial
behavior.

The execution plan's live inventory contains eight retained command-bearing
workflows and two deleted workflows. The older story text says seven. The
execution plan and live checker are used as the current objective inventory;
the discrepancy is preserved as an open documentation finding rather than
silently rewritten.

Prior scoped audits incorporated here:

- `V2.10.S6-security-audit.md` — SEC-01/SEC-02 fixed; SEC-03 through SEC-05
  deferred; native macOS/Windows evidence unavailable; security aggregate pass.
- `V2.10.S6-telemetry-audit.md` — runtime telemetry is N/A; OBS-03 action
  allowlist mismatch deferred to S8.
- `V2.10.S6-maintainability-audit.md` — MAINT-01/MAINT-02 accepted follow-up
  debt; Linux quality and unit/integration evidence available; native
  cross-platform evidence remains environment-limited.

## Evidence run

Commands were run from `/home/vicgenin/work/git/tickoni` on Linux on
2026-08-29. Results below distinguish execution from dry-run or unavailable
host coverage.

| Check | Result | Evidence |
|---|---|---|
| `python3 contrib/test/check_ci_command_surface.py --self-test` | PASS | `self-test: PASS`; synthetic forbidden-command and unsafe-input cases rejected. |
| `python3 contrib/test/check_ci_command_surface.py` | PASS | 10 covered files; 8 retained workflows; 2 deleted workflows; `command-surface: PASS`. |
| `bash contrib/test/check-just-recipe-grid.sh` | PASS | `recipe grid OK: 50 qualified recipes`. |
| `just --list` | PASS | 179 output lines. |
| `just --dump` | PASS | 920 output lines. |
| `just test-unit-tk-linux-x86` | PASS | Actual test execution completed; all 69 tests passed. Cached compilation lines were not counted as proof. |
| `just test-integration-tk-linux-x86` | PASS | All integration suites reported `OK` and exit status 0, including process topology/pipeline and CPU-placement coverage. |
| `just test-demo-tk-linux-x86` | PASS | CLI/demo contract and conformance checks completed with `PASS: tickoni CLI contract and tickoni-supervisor demo contract verified`. |
| `just quality-check-all` | PASS | Format, lint, and protobuf quality aggregate exited 0. |
| `just security-check-all` | PASS | Security aggregate exited 0; sanitizer constituents completed. |
| `git diff --check` | PASS | No whitespace errors. |
| `actionlint` | UNVERIFIED | Execution batch was blocked by the approval gate; prior scoped audit records five existing unknown Windows ARM runner-label findings. |
| `yamllint .github/workflows .github/actions` | UNVERIFIED | Execution batch was blocked by the approval gate; prior scoped audit records baseline formatting findings. |
| repository-wide `shellcheck` | UNVERIFIED | Execution batch was blocked by the approval gate; prior scoped audit records baseline findings and a passing focused recipe-grid check. |
| Native macOS/Windows CI lanes | PASS — CI | Covered by the retained CI lanes per the story owner; local execution is `N/A - reason: this is a Linux host`. |

The quality and security recipes may update badge-generated files. Existing
working-tree changes were preserved; this audit does not revert user changes.

## Full closing gate

### 1. Demoable

- [x] Demo command or fixture-backed scenario exists and exercises the
  story-visible behavior: `just test-demo-tk-linux-x86`.
- [x] Demo output addresses the applicable Increment Gate questions: the
  command surface is explicit, qualified recipes are used, deleted workflows
  are absent, and the demo/test operation is deterministic and non-live.
  Account, venue, instrument, amount, and frequency checks are
  `N/A - reason: S6 does not change financial capability or policy semantics`.
- [x] Blocked-flow example is captured: the command-surface self-test rejects
  synthetic direct commands and unsafe action-input interpolation; invalid
  recipe selectors fail closed.
- [x] Deterministic/reproducible: checker, recipe-grid, demo, and Linux test
  invocations use repository fixtures and explicit recipes; no live financial
  or model side effect is involved.

### 2. Tests at every applicable layer

- [x] **Unit:** `just test-unit-tk-linux-x86` passed with all 69 tests passing.
  The story-owned command-surface and recipe-grid tests also passed.
- [x] **Integration:** `just test-integration-tk-linux-x86` passed all reported
  integration suites. S6 changes CI routing only; the integration evidence is
  regression coverage for the preserved Linux build/test boundary.
- [ ] **System:** `N/A - reason: S6 does not change live model-server, tool,
  adapter, or other external-tool behavior.`
- [ ] **E2E:** `N/A - reason: S6 does not change Tickoni topology, workspace,
  sandboxing, or process lifecycle; macOS/Windows CI lane validation is covered
  by CI as workflow evidence, not as Tickoni E2E coverage.`
- [x] New/changed checker behavior is wired into its repository validation
  path: `--self-test` and the live checker both execute successfully; the
  recipe-grid checker is also invoked directly.
- [x] Tests/checker regression coverage was added alongside the S6 command
  surface, including forbidden direct-command and unsafe-input cases.

### 3. Quality and security checks

- [x] **Fail-closed validation:** unknown action recipe selectors and unsafe
  action inputs are rejected before `just`; checker failures return non-zero;
  `set -euo pipefail` and failure propagation are preserved.
- [x] **Forbidden-direct-access:** the command-surface checker self-test and
  live scan reject direct shell/Zig/Make/Python/setup operations in covered CI
  configuration. Tickoni financial hard-boundary access is
  `N/A - reason: S6 does not modify runtime boundary modules.`
- [x] **Malformed envelope/config/manifest handling:** CI action recipe and
  Python-tool inputs are explicit allowlisted selectors and reject unsupported
  values. Financial capability envelopes, provider configs, and adapter
  manifests are `N/A - reason: S6 does not modify those contracts.`
- [x] **`just quality-check-all`:** PASS, exit 0.
- [x] **`just security-check-all`:** PASS, exit 0. External linter findings
  remain separately recorded and were not silently converted to passes.
- [x] **Allocation/unbounded-growth/hot-path safety:** `N/A - reason: S6 is
  CI/build orchestration and introduces no Tickoni event-path allocation or
  runtime state.`

### 4. Evidence artifacts

- [ ] **Audit JSONL samples:** `N/A - reason: S6 changes CI routing only and
  creates no material runtime audit events.`
- [ ] **Replay samples/output:** `N/A - reason: S6 changes no replay capsule,
  replay substitution, or external-effect behavior.`
- [ ] **Approval/rejection samples:** `N/A - reason: S6 changes no financial
  policy outcome or approval-gated action.`
- [ ] **Metrics/diagnostics output:** `N/A - reason: S6 adds or changes no
  `tkmetr`, `tkdiag`, runtime metric, diagnostic, alert, or telemetry signal.`
- [x] **Fixtures/sample configs/generated artifacts:** updated workflow and
  composite-action files, the command-surface checker self-test fixtures, and
  the recipe-grid checker are repository evidence at stable paths; no commit
  was created by this gate.
- [x] **Long logs:** full command output is retained by the execution harness;
  the audit records salient results and does not paste transient logs into the
  story. Native CI logs are owned by the corresponding CI runs.

### 5. Documentation and roadmap reconciliation

- [x] Documentation reconciliation: **PASS with accepted S8 follow-up**. S6 execution
  documentation and plan describe the qualified command surface, but the
  story/CI documentation still contains the seven-versus-eight workflow count
  discrepancy, recorded as MAINT-02 and explicitly deferred to S8 by the
  roadmap/documentation owner.
- [x] Implementation/documentation divergence is flagged explicitly in the
  scoped maintainability audit; it was not silently normalized during closure.
- [ ] `tile-topology.md`, `tile-delivery-status.md`, and the contribution guide:
  `N/A - reason: S6 changes no tile IDs, links, ownership, or
  Tickoni/Firedancer boundary.`
- [x] Roadmap status: `v2.10-s6.md` remains `**Status:** Backlog` because no
  status transition was requested in this gate. This is an explicit status
  decision, not a failed quality-gate condition; parent epic status is not
  changed.

## Story Closure Checklist

- [x] Demo exists and its output answers the applicable Increment Gate
  Checklist questions.
- [x] Unit tests exist and pass for changed behavior.
- [x] Integration tests exist and pass for changed cross-tile/boundary
  behavior.
- [ ] System tests: `N/A - reason: no live-tool boundary changed.`
- [ ] E2E tests: `N/A - reason: no topology/runtime-lifecycle behavior
  changed.`
- [x] `just quality-check-all` and `just security-check-all` pass, or a
  skipped component is explicitly named: **quality/security aggregates pass;
  external actionlint/yamllint/shellcheck findings are baseline and explicitly
  deferred to S8 as recorded above.**
- [x] Evidence for every touched boundary is captured: CI command-surface and
  recipe-grid evidence is captured; audit/replay/approval/metrics evidence is
  `N/A - reason` as listed above; macOS/Windows evidence is covered by CI.**
- [x] Docs and roadmap status are reconciled: the seven-versus-eight inventory
  discrepancy is explicitly recorded and deferred to S8; the story remains
  Backlog by deliberate status decision.**
- [x] All quality-gate items are true before any status transition: **passed
  or explicitly waived/deferred; no status transition performed.**

## Accepted S8 follow-ups (not S6 closure blockers)

1. **OBS-03 — CI/platform owner:** align the explicit
   `setup-public-gh-runner` allowlist with retained matrix values, or change
   workflow inputs to supported recipes; add checker coverage for allowlist
   drift.
2. **SEC-03 through SEC-05 — S8 CI/security owner:** resolve or explicitly
   accept the existing actionlint runner-label, yamllint, and shellcheck
   findings.
3. **MAINT-01/MAINT-02 — S8 CI/documentation owner:** inventory all local
   actions in the checker and reconcile the workflow count across the story,
   `doc/execution/ci.md`, and the plan.

## Final disposition

The requested evidence and quality gate is stored at this path, with every
quality.md boundary represented as passed or explicitly marked N/A/deferred.
The V2.10.S6 quality gate is **accepted**. The roadmap story remains
**Backlog** because no status transition was requested in this task.
