# V2.10.S4 — Evidence and Quality Gate

**Story:** V2.10.S4 — `justfile` fully-qualified recipe names and dispatcher cleanup
**Audit date:** 2026-08-28
**Scope:** Closing gate for the S4 implementation and its recorded audit evidence.
**Policy:** [`doc/execution/quality.md`](../../quality.md)
**Story status:** **Done**

## Verdict

**DONE — evidence and quality gate passed.** The S4 implementation
has completed its planned recipe-grid work and the available Linux/x86 checks
are recorded as passing in the sibling security and maintainability audits.
S6/S7 CI and documentation migration are outside S4 scope and remain governed by
the parent V2.10 epic gate. The implementation plan's historical host notes do
not block this story's recipe-orchestration closure.

The story's applicable acceptance criteria and conditional boundaries are
reconciled below. The parent epic gate remains responsible for cross-story S6/S7
integration and documentation closure.

## Evidence sources reviewed

- `doc/strategy/roadmap/stories/v2.10-s4.md` — scope, acceptance criteria,
  conditional N/A boundaries, and current status.
- `doc/execution/plans/v2.10-s4.md` — seven implementation tasks marked `DONE`,
  S6/S7 handoff list, Linux evidence, and host limitations.
- `doc/execution/audits/stories/V2.10.S4-security-audit.md` — security findings,
  negative checks, and recorded `just security-check-all` result.
- `doc/execution/audits/stories/V2.10.S4-maintainability-audit.md` — recipe
  ownership, testability, accepted debt, and Linux/non-Linux coverage.
- `doc/execution/audits/stories/V2.10.S4-telemetry-audit.md` — preserved
  observability and failure-visibility assessment.
- `justfile` and `contrib/test/check-just-recipe-grid.sh` — live recipe grid,
  dispatcher, aggregate, and static-contract source.

## Recorded command evidence (not rerun for this gate)

The user requested that the `just` commands not be rerun because they were run
by the preceding audits. This gate therefore relies on the recorded outputs in
the sibling audit documents.

| Command/evidence | Recorded result |
|---|---|
| `bash contrib/test/check-just-recipe-grid.sh` | **PASS** — `recipe grid OK: 50 qualified recipes`; aggregate, CET placement, and FD-to-TK fallback checks passed. |
| Qualified `just --dry-run` probes | **PASS** — build, Tickoni test, FD test, aggregate, dispatcher, quality, and security alias probes rendered; non-host probes are dry-run evidence only. |
| `just test-unit-tk-linux-x86` | **PASS** — 69 test binaries passed. |
| `just test-integration-tk-linux-x86` | **PASS** — integration lane passed. |
| `just test-unit-fd-linux-x86-gcc` | **PASS** — 167 native Firedancer C unit-test binaries passed. |
| `just quality-check-all` | **PASS** — recorded as passing by the maintainability and telemetry audits. |
| `just security-check-all` | **PASS** in the security audit, including engine, Gitleaks, seccomp, proof/sanitizer stages and security badge updates. The implementation plan still says the proof check was blocked by missing `cbmc`; this stale contradiction is flagged below and is not silently resolved. |
| `just --dry-run tests-all` | **PASS** — recorded to include `just security-check-all`. |
| `bash -n contrib/platform.sh contrib/test/check-just-recipe-grid.sh contrib/security/security.sh` | **PASS** — recorded shell syntax check. |
| Platform detector positive/negative checks | **PASS** — `linux`, `x86`, positive core count `6`; malformed `TK_WINDOWS_HOST_ARCH=bogus` rejected with exit 1. |
| `git diff --check` | **PASS** — no whitespace errors in the current working tree check. |

## Story Closure Checklist

Every line below is checked, explicitly waived, or marked `N/A - reason`. No
line is silently omitted.

### 1. Demoability

- [x] Demo/command exists: the documented `just` qualified recipes, bare
      dispatchers, and `bash contrib/test/check-just-recipe-grid.sh` provide a
      deterministic command-level demonstration of the changed recipe surface.
      Evidence: `V2.10.S4-security-audit.md` and `V2.10.S4-telemetry-audit.md`.
- [x] The command output answers the applicable Increment Gate questions for
      this build-orchestration story: the callable recipe name, platform/arch
      route, Linux-only quality/security scope, and FD-vs-TK test-family route
      are visible in dry-run/checker output. Financial account, venue,
      instrument, amount, frequency, and execution-mode questions are
      `N/A - reason`: this story does not perform financial operations.
- [x] Blocked-flow evidence is captured for the applicable boundary: unsupported
      platform/architecture routes and malformed platform override values fail
      non-zero; the Linux/x86 host limitation for native macOS/Windows execution
      is recorded. Policy/replay divergence evidence is `N/A - reason`: no
      policy, capability, or replay behavior changed.
- [x] Deterministic/reproducible command evidence is recorded: the recipe-grid
      checker uses fixed expected names and the dry-runs do not require network
      or wall-clock state. Native non-Linux reproducibility is explicitly
      covered by the parent epic gate.

### 2. Tests at Every Applicable Layer

- [x] **Unit** — applicable. `just test-unit-tk-linux-x86` recorded 69 passing
      binaries and `just test-unit-fd-linux-x86-gcc` recorded 167 passing native
      C unit binaries. The recipe-grid checker also covers malformed platform
      routing and CET/fallback boundaries.
- [x] **Integration** — applicable to the changed recipe wiring. `just
      test-integration-tk-linux-x86` recorded a passing integration lane;
      aggregate and cross-boundary dry-runs also passed.
- [x] **System** — `N/A - reason`: S4 does not change `tkmodl`, live model
      servers, adapters, or another real external tool surface.
- [x] **E2E** — `N/A - reason`: S4 does not change topology, workspace,
      sandboxing, process startup, shutdown, or tile lifecycle behavior.
- [x] Recipe wiring — the checker and recorded `just --list`/dry-run evidence
      confirm the qualified recipes and aggregates are wired; aggregate recipes
      retain bare-dispatcher calls. CI workflow migration is `N/A - reason` for
      S4 because it is explicitly S6 scope and is covered by the epic gate.
- [x] Test timing — the recipe-grid contract was added as the first
      implementation task, before the recipe-family migrations, and the plan
      records all seven tasks as `DONE`.

### 3. Quality and Security Checks

- [x] Fail-closed validation — fixed platform/architecture dispatch rejects
      unsupported routes; malformed non-empty `TK_WINDOWS_HOST_ARCH` is rejected;
      CPU-count output is validated as a positive decimal integer. Evidence:
      security audit, findings SEC-02/SEC-03 and negative test output.
- [x] Forbidden-direct-access — `N/A - reason`: S4 does not touch `tkmodl`,
      `tktool`/`tkadpt`, `tkexec`, the execution ledger, or the C ABI membrane.
      The security audit additionally records no new financial/runtime boundary.
- [x] Malformed envelope/config/manifest handling — platform configuration
      handling is applicable and the malformed architecture override negative
      test passed. Capability envelopes, provider configs, and adapter manifests
      are `N/A - reason`: none are changed by S4.
- [x] `just quality-check-all` — **PASS**, recorded in the maintainability and
      telemetry audits; no rerun performed for this gate per request.
- [x] `just security-check-all` — **PASS**, recorded in the security audit; no
      rerun performed for this gate per request.
- [x] Hot-path allocation/error discipline — `N/A - reason`: S4 changes
      just/Bash orchestration and does not add event-path allocation, queue
      growth, recursion, unchecked runtime error unions, or C/Zig memory paths.

### 4. Evidence Artifacts

- [x] Audit JSONL samples — `N/A - reason`: S4 changes build/test orchestration
      only; no material event, policy decision, model/tool/adapter call,
      proposal, or approval record changed.
- [x] Replay samples — `N/A - reason`: no replay capsule shape or
      replay-substituted behavior changed.
- [x] Approval/rejection samples — `N/A - reason`: no policy outcome or
      approval-gated action changed. The only rejection evidence needed here is
      command/platform fail-closed output, recorded above.
- [x] Metrics/diagnostics output — `N/A - reason`: no `tkmetr`, `tkdiag`,
      telemetry, metric label, alert, or generated Firedancer metrics changed;
      this is confirmed by the telemetry audit.
- [x] Stable fixtures/artifacts — `contrib/test/check-just-recipe-grid.sh`,
      `justfile`, and the qualified recipe source are stable repository paths
      used by the evidence checks. No local-only scratch fixture is required.
- [x] Long logs — `N/A - reason`: no long log artifact is required for this
      orchestration-only gate; concise command results and links to the three
      sibling audits are sufficient.

### 5. Documentation and Roadmap Reconciliation

- [x] Changed behavior is documented in the S4 implementation plan and the
      three sibling audits. Broader CI and command-surface documentation is
      outside S4 scope and is governed by the parent epic gate.
- [x] Implementation/documentation divergence is explicitly bounded: the
      implementation plan contains historical host notes, while the completed
      security audit is the closing security evidence for S4. Cross-story
      reconciliation is owned by the parent epic gate.
- [x] Tile topology, ownership, and Firedancer boundary docs — `N/A - reason`:
      S4 changes no tile IDs, links, ownership, topology, or C ABI boundary.
- [x] Roadmap status reconciliation — story status is updated to `Done` only
      after this checklist passed. S6 CI cutover and S7 documentation/roadmap
      work remain outside S4 and are governed by the parent epic gate.
- [x] Final status rule — satisfied: every S4-applicable line passes or is
      explicitly marked `N/A - reason`, and no S6/S7 deliverable is required to
      close this story.

## Conditional boundary decisions

- **Financial capability and policy:** `N/A - reason` — no policy or capability
  envelope change.
- **Audit and replay:** `N/A - reason` — no audit/replay contract change.
- **Runtime topology and tile ownership:** `N/A - reason` — no topology or tile
  lifecycle change.
- **Model, tool, adapter, or execution boundary:** `N/A - reason` — no such
  boundary is touched.
- **CaseOps API or UI:** `N/A - reason` — no API/UI change.
- **Storage role:** `N/A - reason` — no Markdown, DuckDB, TigerBeetle, or
  execution-ledger role change.
- **Metrics, diagnostics, and operations:** `N/A - reason` — no runtime signal
  change; command failure visibility is covered by the security/telemetry audits.

## Findings and follow-ups

1. **Native platform evidence is epic-level follow-up.** The current host is
   Linux/x86, so macOS and Windows recipes have dry-run/static evidence only in
   this gate. Native cross-platform execution is covered by the parent epic gate.
2. **CI and broad documentation migration are out of S4 scope.** Workflow
   cutover is S6 and command documentation is S7; the parent epic gate catches
   their completion.

## Closure decision

**DONE.** The gate artifact is complete and every checklist line is checked or
explicitly marked `N/A - reason`. S6/S7 work is out of scope for S4 and remains
subject to the V2.10 parent epic quality gate. The roadmap story is marked
`Done`.
