Current state

- contrib/test/ensure_llama_cpp.sh (165 lines) — clones + builds llama.cpp at test time
- contrib/test/ensure_llama_cpp_win.sh (596 lines) — same but with MSVC/SDK/patch complexity
- contrib/test/ensure_hf_model.sh (144 lines) — downloads GGUF model at test time
- contrib/test/llama_cpp_env.sh (56 lines) — shared env helpers
- contrib/test/run_system_model_tests.sh — calls ensure scripts, starts server, runs tests
- contrib/test/run_system_model_tests_win.sh — same for Windows
- contrib/setup/tool-versions.json — already has llama-cpp and huggingface-cli entries in llm-server category
- contrib/setup/install/strategies/build.py — has _build_llama_cpp() but it's a barebones Linux-only clone+cmake
- CI justfile already calls orchestrator.py llm-server in setup targets

What needs to happen

1. Move install logic into setup — Replace the current _build_llama_cpp strategy with proper platform-aware logic (use the shell scripts as the install backend, not the test directory)
2. Move llama_cpp_env.sh to contrib/setup/ (it's shared env helpers, not test-specific)
3. Move the model download into setup via the huggingface-cli pip install + a post-install step
4. Clean up test scripts — replace ensure_llama_cpp.sh/ensure_llama_cpp_win.sh/ensure_hf_model.sh calls with verify-only checks (binary exists, model exists) or remove them entirely
5. Add platform-specific install strategies — Windows needs MSVC/clang path, SDK tool aliasing, embed.cpp patching; Linux needs OpenBLAS detection
6. Wire llm-server into CI setup lanes — ensure it's called before tests

The big question: should I write a new Python install strategy, or should the install strategy just invoke a shell script? Given the existing build.py already has a build_from_source → script pattern, and the Windows script has 600 lines of complex platform logic, I think the right approach is:

- Linux/macOS: a new Python strategy that does simple git clone + cmake with the right flags
- Windows: invoke the Windows build logic as a proper setup script (not a test script)
- Model download: a post-install step in the orchestrator or a dedicated strategy
The ensure_* scripts are doing clone+build+download at test time, which is slow, flaky, and wrong. They belong in setup. Let me fix this.

Plan

1. Move ensure_llama_cpp.sh, ensure_llama_cpp_win.sh, ensure_hf_model.sh, llama_cpp_env.sh → contrib/setup/
2. Replace the complex Windows build logic with simple cmake preset approach (per llama.cpp docs)
3. Wire them into orchestrator.py as install strategies
4. Update test scripts to verify-only (no clone/build/download)
5. Add llm-server to CI setup steps


1. Rewrite ensure_llama_cpp_win.sh — simple cmake preset approach, no MSVC/SDK patching complexity
2. Simplify ensure_llama_cpp.sh — simple cmake with OpenBLAS, no CUDA/ARM complexity
3. Simplify ensure_hf_model.sh — just install hf CLI and download model
4. Update build.py strategy to call these setup scripts properly
5. Update llama_cpp_env.sh path references
6. Update test scripts to verify-only (no ensure calls)
7. Update justfile CI targets to call orchestrator.py llm-server before tests

Now I need to:
1. Create a proper install strategy for llama.cpp (call the setup script)
2. Handle model download in the orchestrator
3. Update tool-versions.json

Done:
- Moved ensure_llama_cpp.sh, ensure_llama_cpp_win.sh, ensure_hf_model.sh, llama_cpp_env.sh → contrib/setup/
- Rewrote Windows script with cmake presets (no patching, no MSVC SDK complexity)
- Simplified Linux/macOS script (no CUDA/ARM)
- Created new llama_cpp.py install strategy
- Registered it in init.py
- Updated tool-versions.json (llama-cpp + new hf-model tool)
- Updated test scripts to verify-only (no clone/build/download)
- Added llm-server to setup targets (Linux/macOS/Windows)

Done:
- Patched justfile: test-system-tk, test-system-tk-windows-x86, test-system-tk-windows-arm now call orchestrator.py llm-server before running tests
- Patched CI workflow: replaced infra-ensure-llamacpp/infra-ensure-model with setup-build-linux-x86-gcc in tests-xlong.yml Windows system test job; tests-short.yml Windows jobs already delegate to justfile targets which now include orchestrator
