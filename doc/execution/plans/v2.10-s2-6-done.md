# Plan: Move Fixture Path Resolution from Runtime to build.zig

## Problem

Source files contain runtime filesystem path resolution (`std.Io.Dir.cwd().readFileAlloc`) that fails when the test binary runs from `.zig-cache/o/...` (not the repo root). This causes `FileNotFound` errors and requires workarounds like `@src().dir` which breaks in Zig 0.17 (field removed from `SourceLocation`).

The `io` parameter is passed to fixture-loading functions but ignored — `cwd()` bypasses it and uses the OS working directory.

## Current State

### Runtime path resolution in 4+ source files (13 total CWD calls):

| File | CWD calls | Lines |
|------|-----------|-------|
| `src/tickoni/tiles/replay/capsule.zig` | 3 | 108, 194, 254 |
| `src/tickoni/tiles/model/backend.zig` | 1 | 72 |
| `src/tickoni/tiles/adapter/backend.zig` | 1 | 159 |
| `src/tickoni/test/fixtures/investment/fixture_denied_trade.zig` | 1 | 39 |
| `src/tickoni/schema/consumer_money/basket.zig` (test) | 1 | 642 |
| `src/tickoni/schema/consumer_money/thesis.zig` (test) | 1 | 809 |
| `src/tickoni/schema/classification/classification.zig` (test) | 2 | 614, 625 |

### Build.zig already has 45 link/Library calls — centralized correctly.

No C source files in `src/`. This is correct.

## Plan

### Phase 1: Create `fixture_paths` build_options module in build.zig
**DONE** — Added `fixture_opts` build_options module in `build.zig` with comptime constants for all fixture directories (`FIXTURE_INVESTMENT_SCENARIOS`, `FIXTURE_PORTFOLIO`, `FIXTURE_CLASSIFICATION_PROTO`, `FIXTURE_THESIS_PROTO`, `FIXTURE_BASKET_PROTO`).

### Phase 2: Remove `resolvePath()` from capsule.zig
**DONE** — Removed `resolvePath()` function, replaced all 3 `std.Io.Dir.cwd().readFileAlloc()` calls in `loadReplayCapsule()`, `loadModelFixtureContent()`, and `loadPaperFixture()` with `fixture_paths.resolveFixtureFile()`. Functions take `io` parameter for `std.Io.Dir.cwd()` access.

### Phase 3: Apply same fix to model/backend.zig and adapter/backend.zig
**DONE** — `model/backend.zig`: replaced `resolvePath()` and `@src().file` logic with `fixture_paths.resolveFixtureFile()`. `adapter/backend.zig`: removed local `resolvePath()` and `readFixtureFile()`, replaced with `fixture_paths.readFixtureFile()`.

### Phase 4: Move CWD-relative test paths into test-specific build targets
**DONE** — `basket.zig`, `thesis.zig`, `classification.zig` (schema test files): replaced `cwd().readFileAlloc()` with `fixture_paths.readFixturePath()`. `fixture_denied_trade.zig`: replaced with `fixture_paths.readFixtureFile()`.

### Phase 5: Verify build.zig has all C source files and link flags
**DONE** — 45 link/Library calls already centralized under `src/tickoni/c_abi/shim/`. No C source files in `src/` other than the shim. No changes needed.

### Phase 6: Add `zig build verify-fixture-paths` step
**DONE** — Added `verify-fixture-paths` build step using `comptime` iteration over fixture path constants, verifies each directory exists via `b.addSystemCommand(&.{ "test", "-e", path })`. Depends on `check` step.

## Files Changed

| File | Change |
|------|--------|
| `build.zig` | Add `fixture_paths` build_options module, add `verify-fixture-paths` step, wire imports to consumers |
| `capsule.zig` | Delete `resolvePath()`, replace 3 CWD calls with comptime/resolved paths, remove or repurpose `io` param |
| `model/backend.zig` | Replace `io`-ignored CWD call with comptime path |
| `adapter/backend.zig` | Replace `io`-ignored CWD call with comptime path |
| `fixture_denied_trade.zig` | Replace CWD-relative path with comptime constant |
| `basket.zig` (test) | Replace CWD-relative path with comptime constant |
| `thesis.zig` (test) | Replace CWD-relative path with comptime constant |
| `classification.zig` (test) | Replace CWD-relative path with comptime constant |

## Execution Order

1. `build.zig` — create `fixture_paths` build_options module, add verify step
2. `capsule.zig` — most critical (3 CWD calls, `resolvePath()` to delete)
3. `model/backend.zig` — 1 CWD call
4. `adapter/backend.zig` — 1 CWD call
5. All test files with CWD paths (basket, thesis, classification, fixture_denied_trade)
6. Wire `fixture_paths` module imports into all consumers in `build.zig`

## What This Solves

1. `FileNotFound` errors when test binary runs from `.zig-cache/o/...` — paths resolved at compile time, not runtime CWD
2. Zig 0.17 `@src()` compatibility — eliminates `@src().dir` usage entirely
3. CWD-dependent behavior — deterministic paths known at build time
4. `io` parameter being passed but ignored — can be removed from fixture loaders if paths are comptime
5. Missing fixtures caught early via `verify-fixture-paths` build step

## Risks & Mitigations

- **Risk:** Production code loads capsules from arbitrary user paths at runtime.
  **Mitigation:** Only apply comptime resolution to test fixtures. Production capsule loading keeps dynamic path support.

- **Risk:** Fixture directory changes break comptime constants.
  **Mitigation:** `verify-fixture-paths` step fails fast at build time.

- **Risk:** Build.zig grows with many fixture path options.
  **Mitigation:** Group related fixtures into logical constants. Use relative paths from repo root consistently.
