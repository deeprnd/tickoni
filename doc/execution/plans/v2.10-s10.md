# Plan: v2.10-s10 — README Badge Automation — Shield.io URLs from PR Status

**Goal:** Auto-update README badge URLs after CI completes on PR merge to main.

**Architecture:** Two GitHub Actions workflows — `coverage` job in `ci.yml` (runs coverage after unit tests, parallel with security-deep) and `badge-update.yml` (runs on ci.yml completion via `workflow_run`, reads CI check-run statuses via GitHub REST API, constructs dynamic shield.io URLs, and rewrites README.md between marker comments). All triggers use `workflow_run` only — no direct `push` to main.

**Tech Stack:** GitHub Actions (workflow_run triggers), GitHub REST API, Python for badge logic, shield.io CDN (no stored SVGs).

**Badge granularity:** Shields.io is the chosen badge service (not native GitHub badges) because:
- Shields.io supports dynamic coverage percentages (e.g. `72.3%`) which native badges cannot render
- Shields.io supports custom colors, badges, and JSON-based dynamic badges
- All badges use a single, consistent tooling — shields.io URLs with query parameters
- Native GitHub badges (`img.shields.io/badge/...`) are static strings; they cannot carry numeric coverage, conditional colors, or "last successful" semantics without a separate badge-updater script anyway

---

## Current State — What Exists Today

### README badges (4 badges, static URLs)

| Badge | Current URL | State | Marker |
|-------|-------------|-------|--------|
| build | `https://img.shields.io/badge/build-passing-brightgreen` | hardcoded | `<!-- badge:build:start/end -->` |
| unit | `https://img.shields.io/badge/unit%20tests-passing-brightgreen` | hardcoded | `<!-- badge:unit:start/end -->` |
| security | `https://img.shields.io/badge/security-passing-brightgreen` | hardcoded | `<!-- badge:security:start/end -->` |
| cov-tk | `https://img.shields.io/badge/harness%20coverage-unknown-lightgrey` | hardcoded | `<!-- badge:cov-tk:start/end -->` |

All badge URLs are static strings in README.md lines 9, 13, 17, 21.

### Existing scripts (not wired into CI)

| Script | Purpose | Default doc updated |
|--------|---------|---------------------|
| `contrib/tool/readme/refresh-badges.py` | Manual badge updater — takes badge name + exit code/unknown, rewrites README between markers. Also supports `reset-all`. | `doc/execution/testing-tickoni.md` (not README by default) |
| `contrib/tool/readme/run-badged-command.py` | Runs a shell command, updates badges with the result, supports lock file. | README for badge names in `{"build", "unit", "security", "cov-tk"}` |
| `contrib/tool/readme/coverage_report.py` | Parses coverage data (kcov for tk, llvm-cov for fd), writes Istanbul-format JSON, checks thresholds. | `build/coverage/tk/coverage-summary.json` or `build/coverage/fd/coverage-summary.json` |

None of these are invoked by CI workflows. The justfile has `test-cov-tk` and `test-cov-fd` recipes, but no CI job triggers them.

### CI workflow structure

`ci.yml` orchestrates a DAG:
- Stage 1: quality + security-secrets (parallel, cheap pre-build gates)
- Stage 2: build (all platforms)
- Stage 3: unit tests (all platforms)
- Stage 4: integration + system + demo (parallel)
- Stage 5: security-deep
- Stage 6: dev-setup

Coverage (`test-cov-tk`, `test-cov-fd`) is **not in CI**. The justfile has recipes but no CI job triggers them.

### Badge-to-job mapping (from story spec)

| Badge | CI source | Logic |
|-------|-----------|-------|
| build | `build` job in ci.yml (aggregated via `ci-required`) | All platform builds pass |
| unit | `unit` job in ci.yml (aggregated via `ci-required`) | All platform unit tests pass |
| security | `security-secrets` + `security-deep` (aggregated via `Project Security`) | BOTH must pass |
| cov-tk | NEW `coverage` job in ci.yml | Coverage job succeeds |

### Aggregated Step Pattern — Project-Scoped Badges

The current `ci.yml` has individual jobs per concern (build across platforms, unit across platforms, security secrets + 4 deep checks). Badge resolution should read from **aggregated project-level jobs** that summarize each concern, not from individual platform/job entries. This makes badges semantically meaningful and simplifies the badge-update logic.

Three new aggregated jobs are added to `ci.yml`:

| Aggregated Job | Name | Needs | Purpose |
|----------------|------|-------|---------|
| `project-build` | `Project Build` | `build` job | No-op gate that becomes success only when the build job succeeds. Represents "whole project builds" for badge purposes. |
| `project-tests` | `Project Tests` | `unit`, `integration`, `system`, `demo`, `conformance` | No-op gate that becomes success only when all test stages pass. Represents "whole project tests pass." |
| `project-security` | `Project Security` | `security-secrets`, all `security-deep-*` jobs | No-op gate that becomes success only when all security checks pass. Represents "whole project security passes." |

**Implementation:** Each is a single-job workflow using `needs` + `if: always()` that checks the results of its dependencies. If all deps are `success` or `skipped`, the aggregated job succeeds. If any dep failed, it fails. The job itself does nothing (no steps that produce output) — it is purely a DAG aggregation node that appears as a single check in GitHub's check-runs API.

Example shape (all three follow the same pattern):

```yaml
project-build:
  name: Project Build
  needs: build
  if: always() && needs.build.result == 'success' || needs.build.result == 'skipped'
  runs-on: ubuntu-24.04
  steps:
    - name: Build aggregated — OK
      run: echo "Build status: ${{ needs.build.result }}"
```

This pattern gives the badge-update workflow a single check name per badge (e.g. `Project Security`) instead of resolving across 6 separate security-deep jobs. It also prevents the "current PR failing" problem: when badge-update reads check-runs for the merge commit on main, it sees exactly one check per aggregated job.

### Badge Update Logic — Last Successful Merge

**Problem:** During an ongoing PR to main, ci.yml runs on the PR branch (not main). If that PR has a failing check, the badge-update workflow (triggered by `workflow_run` on `branch: main`) must not read PR-branch statuses and mark the README badge as failing. Badges should reflect the **last successful merge to main**, not the current in-flight PR.

**Solution: Query by merge SHA, not by PR branch.**

badge-update.yml triggers via `workflow_run` when ci.yml completes. The `workflow_run` payload contains `github.event.workflow_run.head_sha` — this is the SHA that was merged to main, not the PR head. badge-update.yml resolves check statuses against **this SHA** via the GitHub REST API:

```
GET /repos/{owner}/{repo}/commits/{head_sha}/check-runs
```

Where `head_sha` is from `github.event.workflow_run.head_sha` (the merged commit SHA on main).

This approach is industry-standard:
- **GitHub's own status checks** are anchored to a commit SHA, not a branch. When a PR is merged, GitHub creates a new commit on main with the merged SHA. The ci.yml workflow runs against that SHA. badge-update.yml reads check-runs for that SHA.
- **shields.io dynamic badges** work the same way — they query a specific commit's status, not a branch's current state.
- **Alternative: "last successful" filtering.** If we want to be extra safe (e.g., ci.yml never runs on a particular merge, or the workflow_run payload is stale), we can use the GitHub REST API's `GET /repos/{owner}/{repo}/commits/{sha}/check-suites` endpoint, which groups check runs by suite. We can walk backwards through parent commits on main until we find a suite where all aggregated jobs are `success`. However, this is likely unnecessary — `workflow_run.head_sha` is the exact merged commit, so its check-runs are definitive.

**Decision: Use `workflow_run.head_sha` directly.** This is simpler, more correct, and avoids walking parent commits. The badge-update workflow only fires after ci.yml completes on a merge to main, so the SHA in the payload is the one with the authoritative CI results. If ci.yml fails (any aggregated job fails), the badges correctly show `failing/red`. This is the desired behavior — we want badges to reflect the last merged state.

If the user later wants "last successful" semantics (badges only turn red if the last *successful* merge is red, not if the last merge is a partial failure), that requires walking parent commits. That is a Phase 7 follow-up.

---

## Target State — What Gets Built

### New CI job: coverage

A `coverage` job is added to `ci.yml` (`.github/workflows/_ci-coverage.yml` reusable workflow). It runs after unit tests (same `needs` as `security-deep`) and is **parallel** with `security-deep`. This is the right placement because:
- Coverage needs a build artifact (produced by the `build` job), which is already available after `unit`
- Coverage is heavy (kcov scan) but independent of security-deep's work
- Both can run in parallel to reduce total CI time

### Badge update mechanism

`badge-update.yml` triggers via `workflow_run` (no `push` trigger — all changes go through PR):

**Single trigger — ci.yml completion:**
1. Checkout main with `token: ${{ secrets.GITHUB_TOKEN }}`
2. Get SHA: `${{ github.event.workflow_run.head_sha }}`
3. Fetch check-run statuses via GitHub API (`GET /repos/{owner}/{repo}/commits/{sha}/check-runs`)
4. For each badge, resolve pass/fail/unknown from check-run names:
   - `build` → `Project Build` check must exist and have status `success`
   - `unit` → `Project Tests` check must exist and have status `success`
   - `security` → `Project Security` check must exist and have status `success`
   - `cov-tk` → `coverage` job must exist and have status `success`; read `build/coverage/tk/coverage-summary.json` for percentage
5. Construct dynamic shield.io URLs
6. Commit and push updated README to main

Shield.io URLs are dynamic — no stored SVGs needed. The Python script constructs URLs from badge state and rewrites the `src` attribute between marker comments in `README.md`.

### Trigger chain

```
PR merge to main
  → ci.yml runs (build, unit, security-secrets, security-deep, coverage, etc.)
    → coverage runs (parallel with security-deep) ← produces coverage-summary.json
    → security-deep runs (parallel with coverage)
  → ci.yml completes (all jobs done)
    → badge-update.yml runs ← resolves all badge statuses and updates README
```

All badges update in a single `badge-update.yml` run after ci.yml finishes. No separate coverage-badge workflow. No separate cov-tk trigger.

No `push` triggers exist anywhere in this design. Direct pushes to main are not allowed per repo policy. All workflow execution flows through the PR merge path.

---

## Plan

### Phase 1: Add `coverage` CI job to `ci.yml`

**File:** `.github/workflows/_ci-coverage.yml` (NEW) + `ci.yml` (MODIFY)

**Trigger (internal):** Part of ci.yml DAG — runs after `build` and `unit`, parallel with `security-deep`.

**Purpose:** Run coverage for Tickoni harness as part of CI. Produces `build/coverage/tk/coverage-summary.json`.

**Reusable workflow `_ci-coverage.yml`:**

```yaml
name: Coverage Stage

on:
  workflow_call:

jobs:
  coverage:
    name: Tests / Coverage
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup build toolchain for coverage
        run: just setup-build-linux-x86-gcc

      - name: Download build artifact (build job output)
        uses: actions/download-artifact@v5
        with:
          name: tickoni-build
          path: build/

      - name: Run coverage test
        run: just test-cov-tk

      - name: Generate coverage summary JSON
        run: |
          python3 contrib/tool/readme/coverage_report.py coverage-tk \
            build/coverage/tk \
            build/coverage/tk/coverage-summary.json \
            --config contrib/test/coverage-tk.json
```

**ci.yml changes:**
1. Add `coverage` job with `needs: [plan, build, unit]` and `if` matching `security-deep` pattern
2. Add `coverage` to `ci-required` job's `needs` list
3. Add `coverage` to `dev-setup` job's `needs` list (since dev-setup depends on everything)
4. Update the loop in `ci-required` to include `coverage` in the job list

**Note:** The coverage job downloads the build artifact from the `build` job so `just build-tk` doesn't need to recompile. If no artifact mechanism exists, it builds directly (simpler but slower).

**Verification:** Merge a PR to main and verify `build/coverage/tk/coverage-summary.json` appears in the repo after the CI completes.

### Phase 2: `badge-update.yml` — Badge update workflow

**File:** `.github/workflows/badge-update.yml` (NEW)

**Trigger:**
- `workflow_run` with `workflows: ["CI"]`, `types: [completed]`, `branch: [main]` — triggers after ci.yml completes (all badge types)

**Purpose:** Read CI statuses and update README badge URLs.

**Steps:**

1. Checkout main with `token: ${{ secrets.GITHUB_TOKEN }}` (needed for API reads and push)
2. Get SHA: `${{ github.event.workflow_run.head_sha }}` (resolved from workflow_run payload)
3. Run a Python script to:
   a. Call GitHub API to get check-run statuses for that commit
   b. Resolve each badge status:
      - `build` → `Project Build` check must exist and have status `success`
      - `unit` → `Project Tests` check must exist and have status `success`
      - `security` → `Project Security` check must exist and have status `success`
      - `cov-tk` → `Tests / Coverage` job must exist and have status `success`; read `build/coverage/tk/coverage-summary.json` for percentage
   c. Construct dynamic shield.io URLs using shield.io's public badge API
   d. Rewrite README.md between `<!-- badge:X:start -->` / `<!-- badge:X:end -->` markers
   e. Commit and push the updated README

**Implementation approach:** A single Python script at `contrib/tool/readme/badge_updater.py` that:
- Accepts commit SHA as argument (from `github.event.workflow_run.head_sha`)
- Calls `GET /repos/{owner}/{repo}/commits/{sha}/check-runs` (list check runs for a commit)
- Parses check run names to badge statuses:
  - `build` → check name is "Project Build" (the aggregated build job)
  - `unit` → check name is "Project Tests" (the aggregated test job)
  - `security` → check name is "Project Security" (the aggregated security job)
  - `cov-tk` → check name is "Tests / Coverage" (ci.yml coverage job)
- Reads `build/coverage/tk/coverage-summary.json` for cov-tk percentage
- Calls `replace_badge_block()` from existing `refresh-badges.py` to update README.md
- Commits with `git add README.md && git commit -m "ci: update badges for {sha}" && git push origin main`

**Security:** The GitHub API token has `contents: read` scope by default, which is sufficient for reading check runs. Pushing to main requires `contents: write` scope.

**No commit loop:** badge-update.yml uses `workflow_run` triggers (not `push` triggers), so the badge-update commit itself never re-triggers the workflow. This eliminates the infinite commit loop risk entirely.

**Verification:** Merge a PR that changes CI status (e.g. a doc-only PR that skips build) and verify the build badge shows `unknown/lightgrey`.

### Phase 3: Update `refresh-badges.py` default target

**File:** `contrib/tool/readme/refresh-badges.py`

**Change:** Change the default `doc_path` from `TESTING_DOC_PATH` to `README_PATH`.

**Reason:** The story says "update `README.md` badge URLs" — the script's primary audience is local experimentation, but README is the canonical badge target. The testing doc is a secondary concern.

**Steps:**
1. Line 132: Change `doc_path = doc_path or TESTING_DOC_PATH` → `doc_path = doc_path or README_PATH`
2. Line 147: Same change for `update_badge_unknown`
3. Line 157: Same change for `reset_all_badges`

**Verification:** Run `python3 contrib/tool/readme/refresh-badges.py build 0` and verify it updates README.md (not testing-tickoni.md).

### Phase 4: Documentation updates

**Files:**
- `doc/execution/build.md` — add section documenting the badge-update workflow and the new coverage CI job
- `doc/execution/development.md` — add section documenting badge update mechanism and mark `refresh-badges.py` as local experimentation

**Build.md additions:**
- New section "README Badges" describing:
  - Automated badge updates via `badge-update.yml` (runs via `workflow_run` after ci.yml completes)
  - Coverage CI job in ci.yml (runs parallel with security-deep after unit tests)
  - Local experimentation via `contrib/tool/readme/refresh-badges.py`
  - Badge-to-job mapping table

**Development.md additions:**
- Section on badge automation explaining the workflow
- Note that `refresh-badges.py` is for local experimentation only

### Phase 5: Update `run-badged-command.py` README_BADGES

**File:** `contrib/tool/readme/run-badged-command.py`

**Change:** The script already has `README_BADGES = frozenset({"build", "unit", "security", "cov-tk"})` on line 26 — this is correct per the story spec (4 badges). No change needed here.

**Status:** Already aligned with target scope. ✅

### Phase 2b: Create aggregated project-level jobs in ci.yml

**File:** `.github/workflows/ci.yml`

**Purpose:** Add three no-op aggregation jobs that serve as single check-runs for badge resolution.

**Jobs to add:**

```yaml
# ── PROJECT BUILD: aggregated build badge ──────────────────────────────────
project-build:
  name: Project Build
  needs: [plan, build]
  if: >-
    always() &&
    needs.plan.outputs.core == 'true' &&
    needs.build.result == 'success' ||
    (needs.plan.outputs.core == 'false' && needs.build.result == 'skipped')
  runs-on: ubuntu-24.04
  steps:
    - name: Project Build aggregated
      run: echo "Build aggregated: ${{ needs.build.result }}"

# ── PROJECT TESTS: aggregated test badge ───────────────────────────────────
project-tests:
  name: Project Tests
  needs: [plan, unit, integration, system, demo, conformance]
  if: >-
    always() &&
    needs.plan.outputs.core == 'true' &&
    needs.unit.result == 'success' &&
    needs.integration.result == 'success' &&
    needs.system.result == 'success' &&
    needs.demo.result == 'success' &&
    needs.conformance.result == 'success' ||
    (needs.plan.outputs.core == 'false' && needs.unit.result == 'skipped')
  runs-on: ubuntu-24.04
  steps:
    - name: Project Tests aggregated
      run: echo "Tests aggregated: success"

# ── PROJECT SECURITY: aggregated security badge ────────────────────────────
project-security:
  name: Project Security
  needs:
    [plan, security-secrets,
     security-deep-sanitize, security-deep-seccomp,
     security-deep-proof, security-deep-engine,
     security-deep-sanitize-qt]
  if: >-
    always() &&
    needs.plan.outputs.core == 'true' &&
    needs.security-secrets.result == 'success' &&
    needs.security-deep-sanitize.result == 'success' &&
    needs.security-deep-seccomp.result == 'success' &&
    needs.security-deep-proof.result == 'success' &&
    needs.security-deep-engine.result == 'success' &&
    needs.security-deep-sanitize-qt.result == 'success' ||
    (needs.plan.outputs.core == 'false' && needs.security-secrets.result == 'skipped')
  runs-on: ubuntu-24.04
  steps:
    - name: Project Security aggregated
      run: echo "Security aggregated: ${{ needs.security-secrets.result }}"
```

**ci-required update:** Add `project-build`, `project-tests`, `project-security` to `ci-required`'s `needs` list, the job loop, and the case statement. Replace individual job references in the loop with aggregated job references for badge-relevant checks.

**Shield.io badge URL format:** Each badge becomes a dynamic shield.io URL:

```
https://img.shields.io/badge/<label>-<status>-<color>
```

- `build` → `https://img.shields.io/badge/build-passing-brightgreen`
- `unit` → `https://img.shields.io/badge/unit%20tests-passing-brightgreen`
- `security` → `https://img.shields.io/badge/security-passing-brightgreen`
- `cov-tk` → `https://img.shields.io/badge/harness%20coverage-72.3%-orange` (percentage from coverage-summary.json)

For cov-tk, the Python script reads the coverage percentage from `build/coverage/tk/coverage-summary.json` and constructs a URL like:
```
https://img.shields.io/badge/coverage-72.3%-orange
```
Color logic: ≥80% green, ≥60% orange, <60% yellow/orange, 0% or unknown grey.

**README_BADGES update:** The `run-badged-command.py` README_BADGES frozenset should include the three new badge names: `{"build", "unit", "security", "cov-tk"}`. No change needed — this is already correct.

### Phase 6: Update `ci.yml` `ci-required` aggregation

**File:** `.github/workflows/ci.yml`

**Changes:**
1. Add `project-build`, `project-tests`, `project-security` to `ci-required`'s `needs` list
2. Add `coverage` to `ci-required`'s `needs` list (from Phase 1)
3. Add all new jobs to `dev-setup` job's `needs` list
4. Update the job loop in the `ci-required` check step to include `project-build`, `project-tests`, `project-security`, and `coverage`
5. Update `expected_skipped` count to account for the 3 new jobs + coverage job
6. Update the results string in the `ci-required` case statement for all new jobs
7. For badge-relevant checks, prefer aggregated jobs (`project-build`, `project-tests`, `project-security`) over individual platform jobs in the loop

---

## File Change Summary

| File | Action | Changes |
|------|--------|---------|
| `.github/workflows/_ci-coverage.yml` | **Create** | Reusable coverage workflow: build-tk (or download artifact), run kcov, produce summary JSON |
| `.github/workflows/ci.yml` | **Modify** | Add `coverage` job, add `project-build`/`project-tests`/`project-security` aggregated jobs, update `ci-required` and `dev-setup` needs |
| `.github/workflows/badge-update.yml` | **Create** | Badge updater workflow: reads CI statuses, constructs shield.io URLs, rewrites README.md |
| `contrib/tool/readme/badge_updater.py` | **Create** | Python script: GitHub API client, badge resolution, README rewriting, commit+push |
| `contrib/tool/readme/refresh-badges.py` | **Modify** | Change default doc_path from testing-tickoni.md to README.md (lines 132, 147, 157) |
| `doc/execution/build.md` | **Modify** | Add "README Badges" section + coverage CI job documentation |
| `doc/execution/development.md` | **Modify** | Add badge automation documentation |
| `README.md` | **No change** | Badge markers already in place; badge-update.yml will update them |

---

## Execution Order

1. Phase 3: Update `refresh-badges.py` default target (smallest change, 1 file)
2. Phase 1: Add `coverage` job to ci.yml (new _ci-coverage.yml + ci.yml edits)
3. Phase 2b: Create aggregated project-level jobs (`project-build`, `project-tests`, `project-security`) in ci.yml
4. Phase 2: Create `badge_updater.py` script (core logic, test locally)
5. Phase 2a: Create `badge-update.yml` (depends on Phase 2, reads ci.yml check-runs)
6. Phase 6: Update `ci.yml` ci-required aggregation (depends on Phase 1 + Phase 2b)
7. Phase 4: Documentation updates
8. Phase 5: Verification — merge a PR and observe badges update

---

## Risks

- **GitHub API rate limits:** Unauthenticated reads are 60 req/hr; authenticated are 5000/hr. The workflow runs once per PR merge to main, so rate limits are not a concern. Use `GITHUB_TOKEN` with `contents: read` scope.
- **No commit loop:** badge-update.yml uses `workflow_run` triggers (not `push` triggers), so the badge-update commit itself never re-triggers the workflow. This eliminates the infinite commit loop risk entirely — no need to exclude bot commits.
- **No race condition:** `workflow_run` triggers fire only after the upstream workflow completes, so ci.yml finishes all jobs (build, unit, security-deep, coverage, etc.) before badge-update runs. No race condition possible.
- **Coverage is heavy:** kcov coverage scan takes significant time. Running it in the main CI pipeline adds ~5–10 minutes. However, it runs parallel with security-deep, so it doesn't extend total CI time. This is preferable to a separate workflow because all badge statuses resolve from one trigger.
- **Coverage needs build artifacts:** The coverage job needs the compiled binaries to run `just test-cov-tk`. Either the coverage job downloads the build artifact from the `build` job, or it runs `just build-tk` itself. The former is faster; the latter is simpler.
- **workflow_run branch filter:** The `workflow_run` trigger filters on `branch: [main]` to ensure badge-update.yml only runs after PR merges to main, not on PR branches or forks. GitHub's `workflow_run` `branch` filter restricts to the branch that triggered the original workflow.

---

## Verification Commands

1. **Local badge updater test:**
   ```bash
   cd /home/vicgenin/work/git/tickoni
   python3 contrib/tool/readme/badge_updater.py --all --dry-run <commit-sha>
   # Should print the URLs that would be written without modifying README.md
   ```

2. **Coverage summary test:**
   ```bash
   python3 contrib/tool/readme/coverage_report.py coverage-tk <kcov-output-dir> build/coverage/tk/coverage-summary.json --config contrib/test/coverage-tk.json
   cat build/coverage/tk/coverage-summary.json
   ```

3. **Badge refresh test:**
   ```bash
   python3 contrib/tool/readme/refresh-badges.py build 0
   python3 contrib/tool/readme/refresh-badges.py cov-tk 0
   python3 contrib/tool/readme/refresh-badges.py reset-all
   ```

4. **Full workflow test:** Merge a PR to main, wait for ci.yml to complete (including coverage and security-deep), then verify README badges reflect actual CI status.

---

## Acceptance Criteria Checklist

- [ ] `coverage` job exists in ci.yml and runs parallel with security-deep after unit tests
- [ ] `project-build` aggregated job exists in ci.yml (needs `build` job, no-op steps)
- [ ] `project-tests` aggregated job exists in ci.yml (needs `unit`+`integration`+`system`+`demo`+`conformance`, no-op steps)
- [ ] `project-security` aggregated job exists in ci.yml (needs `security-secrets`+all `security-deep-*`, no-op steps)
- [ ] `ci-required` includes `coverage` and all 3 aggregated jobs in its needs list
- [ ] `dev-setup` includes `coverage` and all 3 aggregated jobs in its needs list
- [ ] `badge-update.yml` triggers on ci.yml completion via `workflow_run`
- [ ] Badge-update reads check-runs from `workflow_run.head_sha` (merged commit, not PR branch)
- [ ] Build badge reads from `Project Build` check (not individual platform build jobs)
- [ ] Unit badge reads from `Project Tests` check (not individual platform unit jobs)
- [ ] Security badge reads from `Project Security` check (not individual security jobs)
- [ ] Build badge shows `passing/brightgreen` when all platform builds pass
- [ ] Build badge shows `failing/red` when any platform build fails
- [ ] Unit badge shows `passing/brightgreen` when all platform unit tests pass
- [ ] Unit badge shows `failing/red` when any platform unit test fails
- [ ] Security badge shows `passing/brightgreen` when all security checks pass
- [ ] Security badge shows `failing/red` when any security check fails
- [ ] Cov-tk badge shows real coverage percentage (e.g. `72.3%/orange`) from coverage-summary.json when coverage job succeeds
- [ ] Cov-tk badge shows `unknown/lightgrey` when coverage job fails or has no data
- [ ] Shield.io is the badge service for all badges (consistent tooling, supports dynamic coverage)
- [ ] Badge-update does not create an infinite commit loop (uses `workflow_run` triggers, not `push`)
- [ ] `refresh-badges.py` documents that it's for local experimentation
- [ ] `doc/execution/build.md` documents the badge-update mechanism and coverage CI job
- [ ] `doc/execution/development.md` documents the badge-update mechanism
- [ ] No `push` triggers exist in badge-update.yml
- [ ] Coverage is parallel with security-deep (both triggered after unit tests)
