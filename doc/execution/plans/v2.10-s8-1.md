# Refactor: Tool-scoped setup for CI lanes

**Status:** Plan
**Created:** 2026-08-29
**Scope:** `contrib/setup/` scripts — split monolithic platform scripts into category-specific helpers, add category dispatch to `justfile`, update CI workflows to request only needed tools.
**Related:** [v2.10-s8-2](./v2.10-s8-2.md) — establishes the setup/build boundary; this plan sits on top of that boundary.

---

## Problem Statement

Every CI lane currently installs the full developer environment through `just setup-linux-x86-gcc` or similar, which pulls in compilers, Zig, OpenSSL, quality tools, coverage tools, security tools, and ops tooling — even when a lane only needs a subset. This makes setup slow, opaque (hard to audit what a lane installs), and fragile (a change to one tool's install script can break an unrelated lane).

The setup scripts accept no parameters and unconditionally call every `ensure_*` helper in `common.sh`. There is no way for a CI lane to request "just compilers + Zig" without also getting shellcheck, actionlint, kcov, gitleaks, buf, pre-commit, OpenSSL, and PowerShell.

---

## Target State

### Principle

Every CI lane installs exactly the tools it needs. The `justfile` exposes per-category setup recipes. `just setup-env` remains the developer convenience aggregator. CI workflows call the smallest explicit set of setup recipes.

### Setup Categories

| Category | Tools |
|---|---|
| essential | curl, git, python3, pipx, python3-pip |
| toolchain | just, clang, winget, choco, strawberry perl |
| zig | Zig (install-zig.py) |
| ssl | OpenSSL (install-openssl.sh) |
| fd | snappy, rockdb |
| quality | shellcheck, actionlint, yamllint, pre-commit |
| secrets | gitleaks |
| coverage | kcov |
| security | CBMC toolchain |
| proto | buf (Go toolchain) |
| ops | PowerShell (pwsh) |
| build | make, cmake, zstd, pkg-config |
| mvsc | MSVC (cl) — Windows-only |

### New Directory Structure

```
contrib/setup/
  helpers/
    common.sh                    # modified — all ensure_* functions check $SETUP_CATEGORIES
    install-zig.py               # unchanged
    install-openssl.sh           # unchanged
  linux-x86-essential.sh         # new — calls common.sh with categories=essential
  linux-x86-toolchain.sh         # new — calls common.sh with categories=toolchain
  linux-x86-zig.sh              # new — calls common.sh with categories=zig
  linux-x86-ssl.sh              # new — calls common.sh with categories=ssl
  linux-x86-fd.sh               # new — calls common.sh with categories=fd
  linux-x86-build.sh            # new — calls common.sh with categories=build
  linux-x86-quality.sh          # new — calls common.sh with categories=quality
  linux-x86-secrets.sh          # new — calls common.sh with categories=secrets
  linux-x86-coverage.sh         # new — calls common.sh with categories=coverage
  linux-x86-security.sh         # new — calls common.sh with categories=security
  linux-x86-proto.sh            # new — calls common.sh with categories=proto
  linux-x86-ops.sh              # modified — calls common.sh with categories=ops
  linux-arm-essential.sh        # new (same pattern, arm toolchain)
  linux-arm-toolchain.sh
  linux-arm-zig.sh
  linux-arm-ssl.sh
  linux-arm-fd.sh
  linux-arm-build.sh
  linux-arm-quality.sh
  linux-arm-secrets.sh
  linux-arm-coverage.sh
  linux-arm-security.sh
  linux-arm-proto.sh
  linux-arm-ops.sh              # modified
  macos-x86-essential.sh        # new (same pattern)
  macos-x86-toolchain.sh
  macos-x86-zig.sh
  macos-x86-ssl.sh
  macos-x86-fd.sh
  macos-x86-build.sh
  macos-x86-ops.sh              # modified — quality/security/coverage/secrets are linux-only; proto not on macOS CI
  macos-arm-essential.sh        # new (same pattern)
  macos-arm-toolchain.sh
  macos-arm-zig.sh
  macos-arm-ssl.sh
  macos-arm-fd.sh
  macos-arm-build.sh
  macos-arm-ops.sh              # modified — quality/security/coverage/secrets are linux-only; proto not on macOS CI
  windows-x86.ps1               # modified — accept -Categories param
  windows-arm.ps1               # modified — accept -Categories param
  windows-x86-build.ps1          # new — calls common.sh with categories=build
  windows-arm-build.ps1          # new — calls common.sh with categories=build
  windows-x86-mvsc.ps1          # new — calls common.sh with categories=mvsc
  windows-arm-mvsc.ps1          # new — calls common.sh with categories=mvsc
  README.md                     # updated
  tool-versions.json            # unchanged
```

### Key Design Decisions

1. **`common.sh` is the single source of truth for tool install logic.** No code duplication — each `ensure_*` function already has idempotency checks (`tool_exists`). We add one more check: does its category appear in `$SETUP_CATEGORIES`?

2. **Platform scripts call `common.sh` with a category list.** A category script like `linux-x86-essential.sh` does:
   ```bash
   SETUP_CATEGORIES="essential" bash contrib/setup/helpers/common.sh
   ```
   `common.sh` sources itself but filters install logic based on the variable.

3. **The `justfile` maps one recipe per category per platform.** No aggregation in the justfile — each recipe is independent and auditable. Windows includes a `mvsc` category that does not exist on other platforms.

4. **`setup-env` is the developer convenience aggregator.** It's the only place that aggregates all categories into one recipe. It calls `just <category-recipe> ...` for each category on the current platform — no script accepts an "all" flag; aggregation is done at the `just` level.

5. **CI workflows call individual category recipes.** Example: `just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-zig setup-linux-x86-ssl setup-linux-x86-fd`.

---

## Implementation Plan

### Phase 1: Modify `common.sh` to accept categories

**File:** `contrib/setup/helpers/common.sh`

Add a `$SETUP_CATEGORIES` variable (space-delimited list of category names). Every `ensure_*` function checks if its category is in the list before proceeding.

Each tool gets assigned to exactly one category:

```bash
# Category assignment table (documented at top of file):
#   essential:    ensure_curl, ensure_git, ensure_python3,
#                 ensure_python3_pip, ensure_pipx
#   toolchain:    ensure_just, ensure_clang, ensure_winget, ensure_choco,
#                 ensure_strawberry_perl
#   build:        ensure_make, ensure_cmake, ensure_zstd, ensure_pkgconfig
#   mvsc:         ensure_msvc — Windows-only
#   zig:          ensure_zig
#   ssl:          ensure_openssl
#   fd:           ensure_snappy, ensure_rockdb
#   quality:      ensure_shellcheck, ensure_actionlint,
#                 ensure_precommit, ensure_yamllint
#   coverage:     ensure_kcov
#   security:     ensure_gitleaks, ensure_cbmc_toolchain
#   proto:        ensure_buf
#   ops:          ensure_pwsh
```

**Rules:**
- If `$SETUP_CATEGORIES` is unset or empty, all categories are implied (preserves current behavior for direct script invocations and local dev).
- If `$SETUP_CATEGORIES` is set, only tools whose category appears in the list are installed.
- A tool can belong to multiple categories (e.g., `ensure_gitleaks` belongs to both `secrets` and `security`). It installs once (idempotent).
- `read_packages` and `read_tool_version` are pure lookup functions — they are always available regardless of categories.
- `ensure_just` and `ensure_gcc`/`ensure_clang` need sudo — the category dispatch preserves this.

**Implementation approach:** Each `ensure_*` function gets a `TOOL_CATEGORY` variable at the top. The function wraps its logic in:
```bash
if [ -n "${SETUP_CATEGORIES:-}" ]; then
    category_found=false
    for cat in $SETUP_CATEGORIES; do
        if [ "$cat" = "$TOOL_CATEGORY" ]; then
            category_found=true; break
        fi
    done
    if [ "$category_found" = false ]; then
        log_info "${TOOL}: not requested (${SETUP_CATEGORIES})"
        return 0
    fi
fi
```

### Phase 2: Create category-specific platform scripts

For each platform and each category, create a new script that sets `SETUP_CATEGORIES` and delegates to `common.sh`. Quality, coverage, security, and secrets are **linux-only** (shellcheck, gitleaks, CBMC, kcov don't run on macOS/Windows). Windows also needs `mvsc` scripts. Proto is not needed on Windows CI.

**Example — `contrib/setup/linux-x86-essential.sh`:**
```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" && pwd)"
source "${SCRIPT_DIR}/helpers/common.sh"

SETUP_CATEGORIES="essential" bash "${SCRIPT_DIR}/helpers/common.sh"
```

**Example — `contrib/setup/linux-x86-quality.sh`:**
```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" && pwd)"
source "${SCRIPT_DIR}/helpers/common.sh"

SETUP_CATEGORIES="quality" bash "${SCRIPT_DIR}/helpers/common.sh"
```

**Example — `contrib/setup/linux-x86-ops.sh` (modified):**
```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" && pwd)"
source "${SCRIPT_DIR}/helpers/common.sh"

SETUP_CATEGORIES="ops" bash "${SCRIPT_DIR}/helpers/common.sh"
```

**Existing scripts to remove:** `linux-x86-essential.sh` (old version), `linux-x86-ops.sh` (old version), and equivalent arm/macOS files. These are replaced by the category-specific versions.

### Phase 3: Update the justfile

**Delete existing setup recipes:**
- `setup-linux-x86-gcc`, `setup-linux-x86-clang`, `setup-linux-arm-gcc`, `setup-linux-arm-clang`
- `setup-macos-x86`, `setup-macos-arm`
- `setup-windows-x86`, `setup-windows-arm`
- `setup-windows-ci-x86`, `setup-windows-ci-arm`
- `setup-coverage-linux-x86-clang`
- `setup-fd-deps-linux-x86-gcc`, `setup-fd-deps-linux-x86-clang`, `setup-fd-deps-linux-arm-gcc`, `setup-fd-deps-macos-x86`, `setup-fd-deps-macos-arm`
- `test-prep-linux-x86`

**Add new category-based setup recipes:**

```
# ── Linux x86_64: Category-based setup ──────────────────────────────────────

setup-linux-x86-essential:
    bash contrib/setup/linux-x86-essential.sh

setup-linux-x86-toolchain:
    bash contrib/setup/linux-x86-toolchain.sh

setup-linux-x86-zig:
    bash contrib/setup/linux-x86-zig.sh

setup-linux-x86-ssl:
    bash contrib/setup/linux-x86-ssl.sh

setup-linux-x86-fd:
    bash contrib/setup/linux-x86-fd.sh

setup-linux-x86-build:
    bash contrib/setup/linux-x86-build.sh

setup-linux-x86-quality:
    bash contrib/setup/linux-x86-quality.sh

setup-linux-x86-secrets:
    bash contrib/setup/linux-x86-secrets.sh

setup-linux-x86-coverage:
    bash contrib/setup/linux-x86-coverage.sh

setup-linux-x86-security:
    bash contrib/setup/linux-x86-security.sh

setup-linux-x86-proto:
    bash contrib/setup/linux-x86-proto.sh

setup-linux-x86-ops:
    bash contrib/setup/linux-x86-ops.sh

# ── Linux ARM64: Category-based setup ───────────────────────────────────────

setup-linux-arm-essential:
    bash contrib/setup/linux-arm-essential.sh

setup-linux-arm-toolchain:
    bash contrib/setup/linux-arm-toolchain.sh

setup-linux-arm-zig:
    bash contrib/setup/linux-arm-zig.sh

setup-linux-arm-ssl:
    bash contrib/setup/linux-arm-ssl.sh

setup-linux-arm-fd:
    bash contrib/setup/linux-arm-fd.sh

setup-linux-arm-build:
    bash contrib/setup/linux-arm-build.sh

setup-linux-arm-quality:
    bash contrib/setup/linux-arm-quality.sh

setup-linux-arm-secrets:
    bash contrib/setup/linux-arm-secrets.sh

setup-linux-arm-coverage:
    bash contrib/setup/linux-arm-coverage.sh

setup-linux-arm-security:
    bash contrib/setup/linux-arm-security.sh

setup-linux-arm-proto:
    bash contrib/setup/linux-arm-proto.sh

setup-linux-arm-ops:
    bash contrib/setup/linux-arm-ops.sh

# ── macOS x86_64: Category-based setup ──────────────────────────────────────
# Note: quality, coverage, security, and secrets are linux-only CI lanes
# (shellcheck, gitleaks, CBMC, kcov don't run on macOS/Windows)
# Proto not needed on macOS CI.

setup-macos-x86-essential:
    SECURITY=off bash contrib/setup/macos-x86-essential.sh

setup-macos-x86-toolchain:
    bash contrib/setup/macos-x86-toolchain.sh

setup-macos-x86-zig:
    bash contrib/setup/macos-x86-zig.sh

setup-macos-x86-ssl:
    bash contrib/setup/macos-x86-ssl.sh

setup-macos-x86-fd:
    bash contrib/setup/macos-x86-fd.sh

setup-macos-x86-build:
    bash contrib/setup/macos-x86-build.sh

setup-macos-x86-ops:
    bash contrib/setup/macos-x86-ops.sh

# ── macOS ARM64: Category-based setup ───────────────────────────────────────
# Note: quality, coverage, security, and secrets are linux-only CI lanes
# Proto not needed on macOS CI.

setup-macos-arm-essential:
    SECURITY=off bash contrib/setup/macos-arm-essential.sh

setup-macos-arm-toolchain:
    bash contrib/setup/macos-arm-toolchain.sh

setup-macos-arm-zig:
    bash contrib/setup/macos-arm-zig.sh

setup-macos-arm-ssl:
    bash contrib/setup/macos-arm-ssl.sh

setup-macos-arm-fd:
    bash contrib/setup/macos-arm-fd.sh

setup-macos-arm-build:
    bash contrib/setup/macos-arm-build.sh

setup-macos-arm-ops:
    bash contrib/setup/macos-arm-ops.sh

# ── Windows x86_64: Category-based setup ────────────────────────────────────
# Note: quality, coverage, security, and secrets are linux-only CI lanes;
# proto not needed on Windows CI

setup-windows-x86-essential:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories essential

setup-windows-x86-toolchain:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories toolchain

setup-windows-x86-mvsc:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86-mvsc.ps1 -Categories mvsc

setup-windows-x86-zig:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories zig

setup-windows-x86-ssl:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories ssl

setup-windows-x86-fd:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories fd

setup-windows-x86-build:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories build

setup-windows-x86-ops:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-x86.ps1 -Categories ops

# ── Windows ARM64: Category-based setup ─────────────────────────────────────
# Note: quality, coverage, security, and secrets are linux-only CI lanes;
# proto not needed on Windows CI

setup-windows-arm-essential:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories essential

setup-windows-arm-toolchain:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories toolchain

setup-windows-arm-mvsc:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm-mvsc.ps1 -Categories mvsc

setup-windows-arm-zig:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories zig

setup-windows-arm-ssl:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories ssl

setup-windows-arm-fd:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories fd

setup-windows-arm-build:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories build

setup-windows-arm-ops:
    powershell -ExecutionPolicy Bypass -File contrib/setup/windows-arm.ps1 -Categories ops

# ── Developer convenience: full setup ───────────────────────────────────────

setup-env:
    #!/usr/bin/env bash
    set -euo pipefail
    case "{{ os }}-{{ arch }}" in
      linux-x86)
        just setup-linux-x86-essential \
             setup-linux-x86-toolchain \
             setup-linux-x86-build \
             setup-linux-x86-zig \
             setup-linux-x86-ssl \
             setup-linux-x86-fd \
             setup-linux-x86-quality \
             setup-linux-x86-coverage \
             setup-linux-x86-secrets \
             setup-linux-x86-security \
             setup-linux-x86-proto \
             setup-linux-x86-ops ;;
      linux-arm)
        just setup-linux-arm-essential \
             setup-linux-arm-toolchain \
             setup-linux-arm-build \
             setup-linux-arm-zig \
             setup-linux-arm-ssl \
             setup-linux-arm-fd \
             setup-linux-arm-quality \
             setup-linux-arm-coverage \
             setup-linux-arm-secrets \
             setup-linux-arm-security \
             setup-linux-arm-proto \
             setup-linux-arm-ops ;;
      macos-x86)
        just setup-macos-x86-essential \
             setup-macos-x86-toolchain \
             setup-macos-x86-build \
             setup-macos-x86-zig \
             setup-macos-x86-ssl \
             setup-macos-x86-fd \
             setup-macos-x86-ops ;;
      macos-arm)
        just setup-macos-arm-essential \
             setup-macos-arm-toolchain \
             setup-macos-arm-build \
             setup-macos-arm-zig \
             setup-macos-arm-ssl \
             setup-macos-arm-fd \
             setup-macos-arm-ops ;;
      windows-x86)
        just setup-windows-x86-essential \
             setup-windows-x86-toolchain \
             setup-windows-x86-build \
             setup-windows-x86-mvsc \
             setup-windows-x86-zig \
             setup-windows-x86-ssl \
             setup-windows-x86-fd \
             setup-windows-x86-ops ;;
      windows-arm)
        just setup-windows-arm-essential \
             setup-windows-arm-toolchain \
             setup-windows-arm-build \
             setup-windows-arm-mvsc \
             setup-windows-arm-zig \
             setup-windows-arm-ssl \
             setup-windows-arm-fd \
             setup-windows-arm-ops ;;
      *)
        echo "unsupported host platform: {{ os }}-{{ arch }}" >&2; exit 1 ;;
    esac
    just setup-git
```

**FD deps setup:** Keep `setup-fd-deps-*` recipes but have them call `contrib/setup/helpers/deps.sh` (already correct path after s8-2). These are standalone and don't need category dispatch since they only handle OpenSSL/RocksDB/Snappy.

### Phase 4: Update CI workflows

Map each CI lane to the minimal category set it needs.

#### build-fd.yml — "Engine Build"

| Job | Categories |
|---|---|
| build-linux-gcc | essential + toolchain + zig + ssl + fd + build |
| build-linux-clang | essential + toolchain + zig + ssl + fd + build |
| build-linux-arm | essential + toolchain + zig + ssl + fd + build |
| build-macos-x86_64 | essential + toolchain + zig + ssl + fd + build |
| build-macos-arm | essential + toolchain + zig + ssl + fd + build |
| build-windows-x86 | essential + toolchain + zig + ssl + fd + build + mvsc |
| build-windows-arm | essential + toolchain + zig + ssl + fd + build + mvsc |

Note: Windows CI lanes use non-VS GitHub Actions images (e.g. `ubuntu-22.04`-based with explicit MSVC via `mvsc`). MSVC tool is installed via the `mvsc` category to avoid the default VS image dependency.

**Before (current):**
```yaml
- uses: ./.github/actions/setup-public-gh-runner
  with:
    fd-deps: 'true'
    compiler: gcc
    setup-recipe: setup-linux-x86-gcc
    deps-recipe: setup-fd-deps-linux-x86-gcc
```

**After:**
```yaml
# Linux lanes
- run: just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-zig setup-linux-x86-ssl setup-linux-x86-fd setup-linux-x86-build
- run: just setup-fd-deps-linux-x86-gcc

# Windows lanes (non-VS image — need mvsc)
- run: just setup-windows-x86-essential setup-windows-x86-toolchain setup-windows-x86-zig setup-windows-x86-ssl setup-windows-x86-fd setup-windows-x86-build setup-windows-x86-mvsc
```

#### build-tk.yml — "Harness Build"

| Job | Categories |
|---|---|
| build-tk-linux | essential + toolchain + zig + ssl + fd + build |
| build-tk-macos-* | essential + toolchain + zig + ssl + fd + build |
| build-tk-windows-* | essential + toolchain + zig + ssl + fd + build + mvsc |

Same category set as build-fd. The `fd-deps` flag is removed; deps installed via the `ssl` and `fd` categories. Windows lanes include `mvsc` for MSVC.

Note: Windows CI uses non-VS GitHub Actions images (e.g. `windows-2025`) — MSVC is installed via the `mvsc` category rather than relying on the default Visual Studio image.

#### tests-short.yml — "Harness Unit Tests" + "Demo Conformance"

| Job | Categories |
|---|---|
| short-tests (Linux) | essential + toolchain + zig + ssl + build |
| short-tests-macos-* | essential + toolchain + zig + ssl + build |
| short-tests-windows-* | essential + toolchain + zig + ssl + build + mvsc |
| test-demo-* | essential + toolchain + zig + ssl + build |
| demo-conformance-* | essential + toolchain + zig + ssl + build |

Note: these lanes need `essential` for curl/git/python3, `toolchain` for just/clang, `build` for make/cmake/zstd/pkg-config (Zig compilation), `zig`, and `ssl`. They do NOT need `quality`, `coverage`, `security`, `proto`, or `ops`. Windows lanes also include `mvsc`. They don't need OpenSSL since they're running pre-built Zig tests against the FD libs (which are built separately via the build-fd-tk-libs action).

Wait — the Zig test lanes run `just test-unit-tk-linux-x86` which calls `zigw.sh build ... test`. Zig itself needs to be installed. The FD libs are built via the `build-fd-tk-libs` action which runs `build-fd`. So the test lane needs: essential + toolchain + zig + ssl + fd + build.

Actually, looking more carefully:
- `test-unit-tk-linux-x86` runs `zigw.sh build -Dtest=true -Dfd-lib-dir={{ fd_tickoni_lib }} test --summary all`
- This requires Zig (installed by `setup-linux-x86-zig`) and `fd_tickoni_lib` (built by `build-fd-tk-libs`)
- The `build-fd-tk-libs` action itself doesn't install tools — it just calls `just build-fd-tk-libs` which uses `just build-fd` → `fd-build-lib.sh`
- `fd-build-lib.sh` needs make, cmake, and the compiler from `build`

So test-unit-tk lanes need: **essential + toolchain + zig + ssl + fd + build**.

#### tests-long.yml — "Engine Unit Tests" (FD C unit tests)

| Job | Categories |
|---|---|
| long-tests | essential + toolchain + zig + ssl + fd + build + coverage + security |

This runs `just test-unit-fd-linux-x86-gcc` which builds FD + runs C unit tests. Needs make, cmake, zstd, pkg-config (build), clang (toolchain), OpenSSL, snappy, rockdb, Zig, kcov (coverage), CBMC (security). No quality tools needed.

#### tests-xlong.yml — "Engine E2E Tests" + "LLM System Tests"

| Job | Categories |
|---|---|
| xlong-tests (E2E) | essential + toolchain + zig + ssl + fd + build |
| system-tests-tk | essential + toolchain + zig + ssl + fd + ops + mvsc |

System tests need llama.cpp which requires ops (PowerShell) and the huggingface_hub pip tool (added via `python-tools` input in the action). Windows CI lanes also include `mvsc`.

#### quality.yml — "Quality Checks"

| Job | Categories |
|---|---|
| checks (all matrices) | essential + toolchain + quality + proto + zig + build |

Quality lane runs format, lint, and proto checks. Needs shellcheck (quality), actionlint (quality), pre-commit (quality), buf (proto), and Zig (for proto generation). Windows lanes also include `mvsc`.

After refactor:
```yaml
- run: just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-quality setup-linux-x86-proto setup-linux-x86-zig
- run: just setup-python-tools "pre-commit"  # pre-commit is in quality category but CI also needs it for pip install
```

Wait — `pre-commit` is in the `quality` category but `ensure_precommit` installs via pip/pipx. The CI workflow has a separate `python-tools: pre-commit` input. After refactor, `setup-linux-x86-quality` handles `ensure_precommit` (which does pip install). The `python-tools` input to the composite action may no longer be needed — pre-commit is covered by the quality category.

Actually, looking at the action, `python-tools` is used by `just setup-python-tools` which does a pip install of the packages. This is separate from `ensure_precommit` which installs via pipx. They serve the same purpose for pre-commit but the action's `python-tools` is a general pip-installer. After refactor, the quality category handles pre-commit via `ensure_precommit` (which uses pipx). The action's `python-tools` input can be removed for pre-commit cases.

For the LLM system tests in tests-xlong, `python-tools: 'huggingface_hub[cli]'` is still needed — that's an ops category tool. We should add `ensure_hf_hub` to the ops category.

#### security.yml — "Security Checks"

| Job | Categories |
|---|---|
| gitleaks | essential + toolchain + secrets |
| checks/SecComp | essential + toolchain + security + build |
| checks/Sanitizers | essential + toolchain + zig + ssl + build + security |

Gitleaks job:
```yaml
- run: just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-secrets
- run: just security-gitleaks-check-linux-x86
```

Sanitizers job:
```yaml
- run: just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-zig setup-linux-x86-ssl setup-linux-x86-build setup-linux-x86-security
- run: just security-sanitize-check-linux-x86
```

#### coverage.yml — "Coverage"

| Job | Categories |
|---|---|
| cov-tk | essential + toolchain + zig + ssl + fd + coverage |
| cov-fd | essential + toolchain + zig + ssl + fd + coverage |

Coverage lane needs kcov (coverage category), Zig, compiler, make, cmake, OpenSSL, snappy, rockdb.

After refactor:
```yaml
- run: just setup-linux-x86-essential setup-linux-x86-toolchain setup-linux-x86-zig setup-linux-x86-ssl setup-linux-x86-fd setup-linux-x86-coverage
- run: just setup-fd-deps-linux-x86-clang
- run: just setup-coverage-linux-x86-clang  # install LLVM 18 + coverage symlinks (still needed)
```

The `setup-coverage-linux-x86-clang` recipe installs clang-18, llvm-18, and sets up update-alternatives. This is separate from `ensure_kcov` and should remain as-is since it's about the coverage toolchain, not kcov itself.

### Phase 5: Update composite action `setup-public-gh-runner`

**File:** `.github/actions/setup-public-gh-runner/action.yml`

The current action takes a single `setup-recipe` parameter and calls `just "$SETUP_RECIPE"`. This needs to change to accept a list of category recipes.

**New inputs:**
```yaml
inputs:
  categories:
    description: Comma-separated list of just setup category recipes to run
    required: true
  fd-deps-recipe:
    description: FD deps recipe (optional)
    required: false
    default: ''
  python-tools:
    description: Space-delimited Python packages to pip install (optional)
    required: false
    default: ''
  apt-packages:
    description: Space-delimited apt packages (for tools not in categories)
    required: false
    default: ''
```

**New step logic:**
```yaml
- name: Install toolchain
  shell: bash
  run: |
    set -euo pipefail
    IFS=',' read -ra CATEGORIES <<< "${{ inputs.categories }}"
    for cat in "${CATEGORIES[@]}"; do
      just "$cat"
    done

- name: Install FD deps
  if: inputs.fd-deps-recipe != ''
  shell: bash
  run: |
    set -euo pipefail
    just ${{ inputs.fd-deps-recipe }}

- name: Install Python tools
  if: inputs.python-tools != ''
  shell: bash
  run: |
    set -euo pipefail
    just setup-python-tools "${{ inputs.python-tools }}"
```

**Migration mapping (old → new):**

| Old setup-recipe | New categories |
|---|---|
| `setup-linux-x86-gcc` | `setup-linux-x86-essential,setup-linux-x86-toolchain,setup-linux-x86-zig,setup-linux-x86-ssl,setup-linux-x86-fd,setup-linux-x86-quality,setup-linux-x86-ops` |
| `setup-linux-x86-clang` | `setup-linux-x86-essential,setup-linux-x86-toolchain,setup-linux-x86-zig,setup-linux-x86-ssl,setup-linux-x86-fd,setup-linux-x86-quality,setup-linux-x86-ops` |
| `setup-linux-arm-gcc` | `setup-linux-arm-essential,setup-linux-arm-toolchain,setup-linux-arm-zig,setup-linux-arm-ssl,setup-linux-arm-fd,setup-linux-arm-quality,setup-linux-arm-ops` |
| `setup-linux-arm-clang` | `setup-linux-arm-essential,setup-linux-arm-toolchain,setup-linux-arm-zig,setup-linux-arm-ssl,setup-linux-arm-fd,setup-linux-arm-quality,setup-linux-arm-ops` |
| `setup-macos-x86` | `setup-macos-x86-essential,setup-macos-x86-toolchain,setup-macos-x86-zig,setup-macos-x86-ssl,setup-macos-x86-fd,setup-macos-x86-ops` |
| `setup-macos-arm` | `setup-macos-arm-essential,setup-macos-arm-toolchain,setup-macos-arm-zig,setup-macos-arm-ssl,setup-macos-arm-fd,setup-macos-arm-ops` |
| `setup-windows-ci-x86` | `setup-windows-x86-essential,setup-windows-x86-toolchain,setup-windows-x86-mvsc,setup-windows-x86-zig,setup-windows-x86-ssl,setup-windows-x86-fd,setup-windows-x86-ops` |
| `setup-windows-ci-arm` | `setup-windows-arm-essential,setup-windows-arm-toolchain,setup-windows-arm-mvsc,setup-windows-arm-zig,setup-windows-arm-ssl,setup-windows-arm-fd,setup-windows-arm-ops` |

### Phase 6: Update CI workflows to use new action

Every workflow step that calls `setup-public-gh-runner` with `setup-recipe` is updated to pass `categories` instead.

**Before:**
```yaml
- uses: ./.github/actions/setup-public-gh-runner
  with:
    fd-deps: 'true'
    compiler: gcc
    setup-recipe: setup-linux-x86-gcc
    deps-recipe: setup-fd-deps-linux-x86-gcc
```

**After:**
```yaml
# Linux lanes (no mvsc needed)
- uses: ./.github/actions/setup-public-gh-runner
  with:
    categories: setup-linux-x86-essential,setup-linux-x86-toolchain,setup-linux-x86-zig,setup-linux-x86-ssl,setup-linux-x86-fd,setup-linux-x86-quality
    fd-deps-recipe: setup-fd-deps-linux-x86-gcc

# Windows lanes (non-VS image — include mvsc)
- uses: ./.github/actions/setup-public-gh-runner
  with:
    categories: setup-windows-x86-essential,setup-windows-x86-toolchain,setup-windows-x86-zig,setup-windows-x86-ssl,setup-windows-x86-fd,setup-windows-x86-mvsc
    fd-deps-recipe: setup-fd-deps-linux-x86-gcc
```

### Phase 7: Verification

**Dry-run verification:** For each representative CI lane, run `just --list | grep setup-<lane-category>` to confirm the recipe exists and shows no side-effect. Then run `just --dry-run <recipe>` to confirm it only executes the expected script.

**Aggregate verification:** `just --dry-run setup-env` should show all category recipes being called.

**Auditable trace:** Each CI workflow should be readable top-to-bottom and the setup command should unambiguously reveal which tools are installed. Example:
```yaml
# This lane installs: essential, toolchain, zig, ssl, fd
- uses: ./.github/actions/setup-public-gh-runner
  with:
    categories: setup-linux-x86-essential,setup-linux-x86-toolchain,setup-linux-x86-zig,setup-linux-x86-ssl,setup-linux-x86-fd
```

### Phase 8: Remove orphan Windows setup script

**File:** `.github/actions/setup-public-gh-runner/scripts/setup-windows.ps1` — delete

This script is a duplicate of the category-based `contrib/setup/` scripts. It installs:
- make → covered by `essential` (`ensure_make`)
- strawberry perl → covered by `toolchain` (`ensure_strawberry_perl`)
- LLVM/Clang → covered by `toolchain` (`ensure_clang`)
- MinGW-w64 → covered by `toolchain` (`ensure_mingw`)
- gitleaks → covered by `secrets` (`ensure_gitleaks`)

No workflow or action references this script directly (it is not called from `action.yml`). The `action.yml` calls `just "$SETUP_RECIPE"` which resolves to the `contrib/setup/` scripts. This file is dead code — remove it to eliminate duplication.

### Phase 9: Clean up composite action legacy inputs

**File:** `.github/actions/setup-public-gh-runner/action.yml` — remove deprecated inputs

Remove inputs that are no longer needed after category-based setup:
- `apt-packages` — no longer used (lane scripts handle apt packages)
- `compiler` — replaced by category dispatch (clang in toolchain, gcc in toolchain for Linux)
- `compiler-version` — replaced by category dispatch
- `fd-deps` — replaced by `fd` category + `fd-deps-recipe`
- `kcov` — replaced by `coverage` category
- `setup-recipe` — replaced by `categories`

Retain inputs:
- `categories` — required, comma-separated recipe list
- `fd-deps-recipe` — optional, standalone FD deps
- `python-tools` — optional, general-purpose pip installer (not category-scoped)

---

## File-by-File Change Summary

| File | Action | Description |
|---|---|---|
| `contrib/setup/helpers/common.sh` | **modify** | Add `$SETUP_CATEGORIES` filtering to all `ensure_*` functions |
| `contrib/setup/linux-x86-essential.sh` | **replace** | New version — delegates to common.sh with categories=essential |
| `contrib/setup/linux-x86-toolchain.sh` | **create** | New — delegates to common.sh with categories=toolchain |
| `contrib/setup/linux-x86-zig.sh` | **create** | New — delegates to common.sh with categories=zig |
| `contrib/setup/linux-x86-ssl.sh` | **create** | New — delegates to common.sh with categories=ssl |
| `contrib/setup/linux-x86-fd.sh` | **create** | New — delegates to common.sh with categories=fd |
| `contrib/setup/linux-x86-build.sh` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/linux-x86-quality.sh` | **create** | New — delegates to common.sh with categories=quality |
| `contrib/setup/linux-x86-secrets.sh` | **create** | New — delegates to common.sh with categories=secrets |
| `contrib/setup/linux-x86-coverage.sh` | **create** | New — delegates to common.sh with categories=coverage |
| `contrib/setup/linux-x86-security.sh` | **create** | New — delegates to common.sh with categories=security |
| `contrib/setup/linux-x86-proto.sh` | **create** | New — delegates to common.sh with categories=proto |
| `contrib/setup/linux-x86-ops.sh` | **replace** | New version — delegates to common.sh with categories=ops |
| `contrib/setup/linux-arm-essential.sh` | **replace** | New version — delegates to common.sh with categories=essential |
| `contrib/setup/linux-arm-toolchain.sh` | **create** | New — delegates to common.sh with categories=toolchain |
| `contrib/setup/linux-arm-zig.sh` | **create** | New — delegates to common.sh with categories=zig |
| `contrib/setup/linux-arm-ssl.sh` | **create** | New — delegates to common.sh with categories=ssl |
| `contrib/setup/linux-arm-fd.sh` | **create** | New — delegates to common.sh with categories=fd |
| `contrib/setup/linux-arm-build.sh` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/linux-arm-quality.sh` | **create** | New — delegates to common.sh with categories=quality |
| `contrib/setup/linux-arm-secrets.sh` | **create** | New — delegates to common.sh with categories=secrets |
| `contrib/setup/linux-arm-coverage.sh` | **create** | New — delegates to common.sh with categories=coverage |
| `contrib/setup/linux-arm-security.sh` | **create** | New — delegates to common.sh with categories=security |
| `contrib/setup/linux-arm-proto.sh` | **create** | New — delegates to common.sh with categories=proto |
| `contrib/setup/linux-arm-ops.sh` | **replace** | New version — delegates to common.sh with categories=ops |
| `contrib/setup/macos-x86-essential.sh` | **replace** | New version — delegates to common.sh with categories=essential |
| `contrib/setup/macos-x86-toolchain.sh` | **create** | New — delegates to common.sh with categories=toolchain |
| `contrib/setup/macos-x86-zig.sh` | **create** | New — delegates to common.sh with categories=zig |
| `contrib/setup/macos-x86-ssl.sh` | **create** | New — delegates to common.sh with categories=ssl |
| `contrib/setup/macos-x86-fd.sh` | **create** | New — delegates to common.sh with categories=fd |
| `contrib/setup/macos-x86-build.sh` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/macos-x86-ops.sh` | **replace** | New version — delegates to common.sh with categories=ops (quality/security/coverage/secrets are linux-only) |
| `contrib/setup/macos-arm-essential.sh` | **replace** | New version — delegates to common.sh with categories=essential |
| `contrib/setup/macos-arm-toolchain.sh` | **create** | New — delegates to common.sh with categories=toolchain |
| `contrib/setup/macos-arm-zig.sh` | **create** | New — delegates to common.sh with categories=zig |
| `contrib/setup/macos-arm-ssl.sh` | **create** | New — delegates to common.sh with categories=ssl |
| `contrib/setup/macos-arm-fd.sh` | **create** | New — delegates to common.sh with categories=fd |
| `contrib/setup/macos-arm-build.sh` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/macos-arm-ops.sh` | **replace** | New version — delegates to common.sh with categories=ops (quality/security/coverage/secrets are linux-only) |
| `contrib/setup/windows-x86.ps1` | **modify** | Accept `-Categories` parameter, dispatch to ensure_* with category check |
| `contrib/setup/windows-arm.ps1` | **modify** | Same as windows-x86.ps1 |
| `contrib/setup/windows-x86-build.ps1` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/windows-arm-build.ps1` | **create** | New — delegates to common.sh with categories=build |
| `contrib/setup/windows-x86-mvsc.ps1` | **create** | New — delegates to common.sh with categories=mvsc |
| `contrib/setup/windows-arm-mvsc.ps1` | **create** | New — delegates to common.sh with categories=mvsc |
| `justfile` | **modify** | Remove old monolithic setup recipes, add per-category setup recipes; update setup-env to delegate to category recipes (quality/security/coverage/secrets only on linux; proto not on macOS/Windows) |
| `.github/actions/setup-public-gh-runner/action.yml` | **modify** | Replace `setup-recipe` with `categories` input; remove deprecated inputs (`apt-packages`, `compiler`, `compiler-version`, `fd-deps`, `kcov`); remove switch/case for old recipe names; update step logic to iterate comma-separated categories |
| `.github/workflows/build-fd.yml` | **modify** | Update all jobs to use new categories |
| `.github/workflows/build-tk.yml` | **modify** | Update all jobs to use new categories |
| `.github/workflows/tests-short.yml` | **modify** | Update all jobs to use new categories |
| `.github/workflows/tests-long.yml` | **modify** | Update jobs to use new categories |
| `.github/workflows/tests-xlong.yml` | **modify** | Update jobs to use new categories |
| `.github/workflows/quality.yml` | **modify** | Update jobs to use new categories (linux-only) |
| `.github/workflows/security.yml` | **modify** | Update jobs to use new categories (linux-only) |
| `.github/workflows/coverage.yml` | **modify** | Update jobs to use new categories (linux-only) |
| `contrib/setup/README.md` | **update** | Document new category-based setup surface |
| `.github/actions/setup-public-gh-runner/scripts/setup-windows.ps1` | **delete** | Remove orphan — duplicate of contrib/setup/ categories (make, strawberry perl, LLVM, MinGW, gitleaks) |

---

## Risks and Mitigations

1. **`ensure_*` functions are called indirectly from multiple paths.** Some CI lanes may call `just setup-linux-x86-gcc` directly (not via the composite action). All such references in the justfile and CI workflows must be updated. **Mitigation:** delete old recipes and verify no remaining references.

2. **The composite action currently handles `fd-deps`, `security`, `kcov`, `zig`, `compiler`, `compiler-version` as separate inputs.** These map to categories but the mapping isn't 1:1. **Mitigation:** the new action takes a flat list of category recipes, removing the need for the mapping.

3. **Windows setup scripts use different syntax (PowerShell).** The category filter must be implemented in the PowerShell `ensure_*` functions. **Mitigation:** Windows scripts already have `if (Test-CommandExists ...)` idempotency checks; add `$categories` check similarly.

4. **`ensure_gitleaks` belongs to both `secrets` and `security` categories.** It must install once regardless of which category triggers it. **Mitigation:** the idempotency check (`tool_exists gitleaks`) handles this — the category filter only gates whether the function runs at all.

5. **`setup-python-tools` is a general-purpose pip installer, not tied to any category.** It handles `huggingface_hub[cli]` and `pre-commit` via pip (not pipx). **Mitigation:** keep `setup-python-tools` as-is in the justfile; it's not part of the category system.

---

## Out of Scope

- Changes to CI lane definitions (which lanes run, what they test) — these stay the same.
- Changes to build recipes or test recipes — these stay the same.
- Changes to Firedancer deps (`deps.sh`) — these remain standalone.
- Changes to `contrib/build/` — out of scope for this plan.
- Changes to `contrib/test/` — out of scope.
- Changes to `contrib/security/security.sh` — out of scope.
- Changes to `contrib/quality/quality.sh` — out of scope.
- Adding new tool categories beyond the 12 defined — this plan is about refactoring existing tool installs, not adding new ones.
