# V3.23: Ship Tickoni as a Verifiable, Installable Runtime

**Labels:** `type/epic`, `area/operations`, `area/platform`, `area/security`, `area/trust`, `type/feature`
**GitHub Issue:** [16](https://github.com/deeprnd/tickoni/issues/16)

<!-- One paragraph: what complete feature this epic delivers and why now. -->

People don't trust code they can't install themselves. Every financial system that claims auditability and replay still asks the buyer to compile from source, run on Linux, or hand over admin credentials just to look inside. That is not a feature gap — it is a trust gap. V3.23 closes it.

V3.23 is where Tickoni stops being a source repository and becomes a downloadable, verifiable, installable product. A Windows or macOS user downloads one file, verifies its checksum, runs an installer that never asks for admin rights, and within seconds has a fully policy-checked, audit-ready runtime with doctor, demo, and replay — all local, all deterministic. The runtime was proven portable in V2.21 and V2.22; V3.23 proves it is accessible.

## Product Intent

A consumer user on any supported platform installs Tickoni without admin privileges, verifies the package against published checksums, runs `tickoni doctor` and a deterministic paper demo, updates safely, and uninstalls cleanly — every installed artifact carrying version, OS, architecture, runtime tier, and isolation tier metadata.

The trust signal most financial products cannot make: download, verify, install, and walk away — no admin rights, no embedded hooks, no live financial credentials. The installer refuses to run on unsupported hosts, never requests broker or model credentials, and disables live execution by default.

### Implementation Constraints

- Per-platform installers (tarball + helper scripts; winget for Windows; no .deb/.rpm or .pkg at this stage)
- Per-user scoped paths (`~/.tickoni/`, `%LOCALAPPDATA%\tickoni\`) — no elevated privileges
- SHA256 checksums published alongside artifacts; signatures documented as follow-on
- Update preserves `~/.tickoni/evidence/` and `~/.tickoni/demos/`; uninstall preserves evidence by default
- Installer telemetry disabled by default; no outbound calls during install
- Unsupported hosts fail closed with an explicit diagnostic

## Users And Jobs

- **Consumer-money user:** Install Tickoni on their laptop, run a paper demo, and see policy-checked proposals and replay proofs without any admin credentials or Linux-only toolchain. The job: "I want to see how financial AI works without giving away my machine."
- **Partner reviewer / auditor:** Download a release artifact, verify its checksum against published hashes, inspect audit and replay evidence locally, and uninstall without losing evidence unless explicitly requested. The job: "I need to prove this system is what you say it is, and I will do it on my own hardware."
- **Developer-community evaluator:** Install a pre-built Tickoni package for their platform, run doctor and demo, compare conformance results against Linux full-runtime, and update when a new version ships. The job: "I want to evaluate Tickoni fairly, on the same desktop I already own."
- **Runtime owner:** Maintain a release pipeline that produces per-platform, per-architecture binary packages with checksums, manages the update lifecycle, and documents the uninstall path. The job: "I need to ship updates without breaking trust or evidence."

## Success Metrics

- A supported platform user can install Tickoni without sudo/admin, run
  `tickoni --version` and `tickoni doctor`, and execute a deterministic paper
  demo on first install.
- Every release artifact includes published SHA256 checksums (and optional
  signatures when the signing infrastructure is available). A user can verify
  checksums before running the installer.
- `tickoni update` or a documented reinstall path preserves `~/.tickoni/evidence/`
  and `~/.tickoni/demos/` while replacing only the runtime binaries and configs.
- `tickoni uninstall` (or documented manual removal) removes binaries and runtime
  files without deleting audit/replay evidence unless the user explicitly requests
  a full reset.
- The installer reports the installed Tickoni version, OS, architecture, runtime
  tier, and isolation tier; the installed `tickoni doctor` output matches the
  release metadata.
- Unsupported hosts fail closed with an explicit diagnostic instead of silently
  installing an unsupported binary.

## Demo Moment

A Windows or macOS user downloads one file, verifies its SHA256, and runs the installer (no sudo). Within seconds, they have a working `tickoni` with version, doctor, and demo — all with explicit platform tier and no live execution.

The installed binary reports its platform tier (macOS retail, Windows retail). `tickoni doctor --plain` confirms the support path. The retail demo produces a deterministic policy-checked proposal, audit JSONL, and replay match — identical to the Linux full-runtime fixture — and everything lives locally on the user's own machine.

## Scope

### In Scope

- Define per-platform installer targets:
  - Linux: tarball (`.tar.gz`) with post-install script or shell alias; winget
    package (`.yaml`) for Windows; no .deb/.rpm at this stage.
  - macOS: tarball (`.tar.gz`) with post-install helper; .pkg is out of scope
    but the tarball path supports manual verification.
  - Windows: tarball (`.tar.gz`) with PowerShell install helper; winget manifest
    (`.yaml`) for per-user install.
- Produce per-platform, per-architecture release binaries:
  - `tickoni` (CLI — version/doctor)
  - `tickoni-supervisor` (demo/audit/replay)
- Publish SHA256 checksums for each release artifact alongside a signed manifest
  or checksum list. If signature infrastructure is not yet available, publish
  checksums only and document the manual verification path.
- Document and implement the update path: download, verify checksum, replace
  binaries, preserve `~/.tickoni/evidence/` and `~/.tickoni/demos/`.
- Document and implement the uninstall path: remove binaries, leave evidence
  intact by default, allow full reset if user opts in.
- Ensure no admin or elevated privileges are required for install, update, or
  uninstall. User-scoped paths (`~/.tickoni/`, `%LOCALAPPDATA%\tickoni\`) are
  the default on all platforms.
- Ensure the installer fails closed on unsupported hosts (wrong OS, wrong
  architecture, too-old Windows) with an explicit diagnostic.
- Ensure installer telemetry is disabled by default; no outbound calls during
  install.
- Ensure the installer does not request broker, payment, crypto, approved execution ledger, or
  live model-provider credentials.

### Out Of Scope

- Signed release artifacts / code-signing infrastructure (key management,
  Sigstore, GPG, minisign). Documented as a follow-on decision.
- App-store distribution (Microsoft Store, Homebrew tap, Linux distro repos).
- Automatic background self-update. Update requires user action (download +
  install or `tickoni update`).
- Cross-platform unified binary. Each platform ships its own build.
- Windows MSI installer or macOS `.pkg` at this stage. `.tar.gz` + helper
  scripts are the default.
- CaseOps integration for release management.
- Multi-architecture single binary (e.g., macOS Universal Binary). Separate
  artifacts per architecture.

## Conditional Boundary Checklist

| Boundary | Status | Notes / linked story |
| --- | --- | --- |
| Financial capability and policy | Applies | Installer does not change policy; paper/sandbox execution defaults preserved. S2 enforces fail-closed on unsupported hosts. |
| Audit records and evidence | Applies | Uninstaller preserves evidence by default. Update preserves evidence. S3 documents evidence safety guarantees. |
| Replay and divergence behavior | Applies | Installed runtime must produce identical replay results to source-build for the same fixture. S2 tests this. |
| Runtime topology, tile ownership, or links | N/A | Epic distributes existing runtime; does not change topology. |
| Model gateway governance (`tkmodl`) | Applies | Installer does not supply model credentials; runtime uses mocks or local governed providers. |
| Tool broker or adapter dispatch (`tktool` / `tkadpt`) | Applies | Installer does not install signed adapters or broker credentials. |
| Approved execution (`tkexec`) | N/A | Live execution disabled on all retail paths. |
| CaseOps API/UI (`tkapi`) | N/A | Installer does not change CaseOps. |
| Storage role: Memory, Analytics, Ledger | Applies | Installed runtime uses `~/.tickoni/evidence/` for local evidence. the approved execution ledger is not accessed by retail demos. |
| Metrics, diagnostics, and operations | Applies | Installer telemetry disabled by default. S4 audits telemetry. |
| Security and fail-closed behavior | Applies | Installer validates host before install, fails closed on unsupported. No secrets during install. Checksum verification required before execution. |

## Story Breakdown

- [ ] V3.23.S1: Release pipeline — produce per-platform, per-architecture tarballs from CI, publish SHA256 checksums, and produce winget manifests.
- [ ] V3.23.S2: Installer — platform-specific install helpers (shell script / PowerShell / macOS post-install), fail-closed host validation, per-user scoped paths, no admin required.
- [ ] V3.23.S3: Update and uninstall — documented update path that preserves evidence, uninstall that removes binaries but preserves evidence by default, full reset opt-in.
- [ ] V3.23.S4: Distribution, verification, docs, and evidence gate — winget packages for Windows, checksum/signature publishing, update to retail-runtime-support.md and platform docs, CI release workflow, evidence artifacts, and roadmap reconciliation.

## Epic Acceptance

- [ ] All required story issues are done or explicitly deferred with rationale.
- [ ] The demo moment succeeds on at least one non-Linux platform (macOS or Windows) using a released artifact, not a source build.
- [ ] Relevant policy, audit, replay, adapter, API/UI, metrics, diagnostics,
      and documentation artifacts are linked from this epic.
- [ ] Non-goals and deferred work are visible in the roadmap.
- [ ] No live model, broker, payment, trading, crypto, approved execution ledger, or execution
      side effects occur unless this epic explicitly enables an approved
      sandbox/live execution path.

## Release / Evidence Gate

- What can the user or operator do now that they could not before this epic?
  Download and install a verified Tickoni package on any supported platform without
  admin rights, run doctor and demo, update, and uninstall — all locally. The user
  owns the runtime on their own machine. Before V3.23, seeing Tickoni meant compiling
  from source and running Linux. After V3.23, it means downloading a file and verifying
  it yourself.

- What changed from the previous roadmap increment?
  V2.21 and V2.22 proved the runtime compiles and runs on macOS and Windows. V3.23
  adds the first distributable binary packages with checksums, install/update/uninstall
  flows, and per-user scoped installation. The runtime went from "compilable on three
  platforms" to "shipped on three platforms."

- What is this epic's wow-effect: the visible moment in this epic's demo or
  workflow that makes Tickoni feel unlike a generic agent harness?
  A Windows or macOS user downloads one file, verifies its SHA256, runs the installer
  (no sudo), and within seconds has a working `tickoni` with version, doctor, and
  demo — all with explicit platform tier and no live execution. The trust surface
  (version, tier, evidence) is now shipped, not just documented.

- How does this epic progress Tickoni's unique value proposition from
  doc/strategy/positioning.md: speed, isolation/control, spend governance,
  policy-gated action, and forensic replay?
  Distributable packages make forensic replay and policy-gated proposals accessible
  to consumers without requiring them to build from source or run a Linux environment.
  The trust surface (version, tier, evidence) is now shipped, not just documented.
  Anyone can now install Tickoni, verify it themselves, and watch a policy-checked
  financial proposal move through audit and replay — on their own machine.

- Which demo command or CaseOps flow closes the epic?
  `~/.tickoni/bin/tickoni doctor --plain` + retail demo manifest from the installed
  package on Windows or macOS.

- Which account, beneficiary, IBAN, wallet, rail, currency, market, venue,
  asset class, instrument, notional, amount, exposure, and frequency checks are
  enforced, if any?
  Paper/sandbox only. No account/beneficiary/rail checks on install. Policy outcomes
  match Linux full-runtime fixture results.

- What happens when the requested action exceeds policy, scope, or evidence?
  Unsupported hosts fail closed with an explicit diagnostic. The installer refuses to
  run on unsupported platforms. No silent downgrade.

- Is execution paper-only, draft-only, sandbox, live, or disabled?
  Paper/sandbox default; live execution is disabled on all retail paths.

- Which fixtures, samples, audit records, replay capsules, screenshots, or API
  examples prove the behavior?
  Installed demo output on Windows/macOS, checksum verification log, doctor output
  from installed binary, uninstall evidence-preservation log.

- Can replay run without external side effects when replay applies?
  Yes — installed runtime produces replay capsules identical to source-build for the
  same fixture inputs.

- What intentional blocked-flow or divergence example proves fail-closed behavior?
  Installer run on an unsupported host (e.g., wrong architecture, too-old Windows)
  fails with a diagnostic and exits non-zero. No binary installed.

## Dependencies And Decisions

- V2.21 (macOS) and V2.22 (Windows) must produce buildable binaries before this
  epic can package them. The release pipeline consumes `zig-out/bin/tickoni` and
  `zig-out/bin/tickoni-supervisor` artifacts.
- `doc/execution/retail-runtime-support.md` already documents the planned install
  layout (`~/.tickoni/bin/`, `~/.tickoni/demos/`, `~/.tickoni/evidence/`), manual
  verification path, update path, uninstall path, and privacy defaults. This epic
  implements what those docs prescribe.
- The support tier model is defined in V2.21.S1/V2.22.S1 (`platform-tiers.md` +
  `tier.zig`). Installers must respect the tier (Linux full, macOS retail, Windows
  retail).
- A signing infrastructure decision (Sigstore, GPG, minisign, platform-native)
  is needed separately. Checksums only are sufficient for V3.23; signing is
  out of scope but documented as a follow-on.
- Distribution channel decisions (winget, Homebrew, Linux distro repos) are
  deferred. V3.23 provides tarballs + winget for Windows as the minimum viable
  distribution surface.
- Minimum version manifest for demos (already defined in S3 of V2.22) must be
  respected by the installed runtime.
