# V5.5: Watchlist Management And Valuation Queue

**Status:** Backlog
**Milestone:** M5
**Labels:** `epic`, `area/investing`, `area/platform`

Watchlist CRUD: add/remove tickers with optional notes. Watchlist tickers
enter the V4.32 DCF valuation queue automatically, producing
`valuation.analysis.propose` capability envelopes with capability checks
(`valuation_market_data.read`, `valuation_financials.read`,
`valuation_analysis.analyze`). Every valuation run is captured as a replay
capsule substituting model and adapter outputs. V5.5 owns the watchlist and
valuation queue; portfolio state is V5.26, exposure is V5.29, rebalance is
V5.4, and model governance is V5.2.

Extracted from the M5 backlog proposal.

## The Why

Investors don't value the companies in their portfolio. They value the ones
they *don't* yet own. Every investment decision starts with a ticker typed
into a blank field — "what if?" — and that moment of curiosity is where
value creation begins. But without institutional-grade valuation infrastructure,
that curiosity dies in a spreadsheet.

This is not a watchlist feature. This is the bridge between curiosity and conviction.

Most retail investors never get a formal valuation on a company they're
considering. They read a blog post, glance at a PE ratio, and decide. The
difference between gambling and investing is not conviction — it's a process
that produces evidence a human can trust, audit, and replay.

Tickoni gives that process to every consumer investor, at no cost, with the
same rigor that institutional analysts charge for. Because the question isn't
"can we build a watchlist?" It's "can we make institutional valuation
accessible to anyone who cares enough to look?"

The answer is yes, and V5.5 is how.

## The What

When a user adds a ticker to their watchlist, Tickoni doesn't just store it.
It triggers an automatic DCF valuation — intrinsic value range, Monte Carlo
confidence interval, full capability checks, audit trail, and replay capsule.
All of it. Without the user lifting a finger.

The psychological effect matters here. A watchlist that *does something*
when you add a ticker feels different from one that just holds text. It's the
difference between a notepad and a dashboard. The value isn't in the CRUD
operations — it's in the moment the user adds "AMZN" and gets back an
institutional-grade valuation record that says "here's what this company
might be worth, here's how sure we are, here's the evidence, and here's
how to prove it."

That moment — the gap between adding a ticker and seeing a valuation — is
where Tickoni earns trust. Not through marketing. Through substance that
shows up in the moment of decision.

## Product Intent

Tickoni has portfolio state (V5.26), computed exposure (V5.29), rebalance
suggestions (V5.4), and model governance (V5.2), but no way to track
companies outside the portfolio and get them valued. V5.5 delivers the
watchlist surface: users add tickers to watch, those tickers automatically
enter the V4.32 valuation queue, and every valuation run produces a
replay capsule with full capability checks and deterministic substitution.
This closes the M5 demo loop: watchlist ticker enters valuation queue,
completes DCF valuation with intrinsic value range and Monte Carlo CI,
and is replayable from captured inputs.

## Users And Jobs

- Consumer investor: add tickers to a watchlist with optional notes, remove
  tickers, see watchlist valuation status. *Job: "I want to explore investment
  ideas without opening a spreadsheet."*
- Portfolio analyst: get automatic DCF-style valuation on every company
  that appears in any portfolio or watchlist, receive proposal-grade valuation
  records with evidence. *Job: "I need valuation evidence I can trust and
  audit, not an API response I can't verify."*
- Risk reviewer: audit watchlist mutations, inspect valuation assumptions,
  verify capability envelope compliance for every valuation queue entry.
  *Job: "I need to prove that every valuation was governed by policy, not
  whim."*
- CaseOps operator: review watchlist entries, valuation evidence cards,
  replay capsules, and the full audit trail from watchlist entry to
  valuation completion. *Job: "I need to reconstruct any valuation decision
  from raw inputs, no matter how long ago it happened."*

## Success Metrics

- A user can add and remove tickers from a watchlist with optional notes.
- Watchlist tickers automatically enter the V4.32 valuation queue with full
  capability checks.
- Every valuation run produces a replay capsule substituting model and adapter
  outputs without calling external services.
- Valuation evidence cards show intrinsic value range and Monte Carlo CI.
- No live model, broker, payment, trading, crypto, approved execution ledger, or execution
  side effects occur unless this epic explicitly enables an approved sandbox path.

## The Demo Moment (The Thing That Changes Everything)

- Command or flow: Add AMZN to the watchlist with note "evaluate for portfolio
  inclusion". Show AMZN entering the V4.32 DCF valuation queue with
  capability checks (`valuation_market_data.read`, `valuation_financials.read`,
  `valuation_analysis.analyze`). Show completed valuation with intrinsic value
  range and Monte Carlo CI. Replay the valuation with substituted outputs.
- Expected result: deterministic demo output showing watchlist entry, valuation
  queue entry with capability checks, completed valuation record, replay
  capsule substituting model and adapter outputs.

The demo isn't a checklist. It's the moment a skeptical observer thinks
"wait, that was *automatic*?" The user added one ticker and Tickoni produced
a complete, auditable, replayable valuation. Not a chart. Not a summary. A
financially-grounded evidence record with bounds and confidence intervals,
ready to be replayed from first principles.

That is a trust signal no feature list can replicate, because trust is
built in moments, not documents.

## Scope

### In Scope

- Watchlist CRUD: add/remove tickers with optional notes.
- Automatic valuation queue: watchlist tickers queued for DCF valuation
  with V4.32 capability checks.
- Valuation record production: intrinsic value range, Monte Carlo CI,
  valuation evidence cards.
- Replay capsule: every valuation run captured as replay capsule substituting
  model and adapter outputs.
- Valuation queue depth and status metrics via `tkmetr`.

### Out Of Scope

- Portfolio CRUD (V5.26).
- Exposure computation (V5.29).
- Target exposure and rebalance suggestions (V5.4).
- Model tier selection (V5.2).
- Live market data ingestion (assumed provided as input/fixture per V4.32).
- Push notifications or device push.
- Social/clone/copy portfolio features (deferred to M7).

## Conditional Boundary Checklist

|| Boundary | Status | Notes / linked story |
|| --- | --- | --- |
|| Financial capability and policy | Applies | `valuation.analysis.propose`, `valuation_market_data.read`, `valuation_financials.read`, `valuation_queue.manage` capability checks. Linked: V5.5. |
|| Audit records and evidence | Applies | Every watchlist mutation is a source event; every valuation run captured as replay capsule. Linked: V5.5. |
|| Replay and divergence behavior | Applies | Replay runs valuation queue entries against same event stream; deterministic seeds and substituted model outputs. Linked: V5.5. |
|| Runtime topology, tile ownership, or links | Applies | `tkdisp` schedules queued valuations as bounded agent runs. Model governance owned by V5.2. Linked: V5.2, V5.5. |
|| Model gateway governance (`tkmodl`) | Applies | Model tier selection stored as `tkmodl` capability scope. Linked: V5.2. |
|| Tool broker or adapter dispatch (`tktool` / `tkadpt`) | Applies | Valuation requires market data and financials reads through `tktool`/`tkadpt`. Linked: V5.5. |
|| Approved execution (`tkexec`) | N/A | Valuation is analysis-only; no execution. |
|| CaseOps API/UI (`tkapi`) | Applies | Watchlist, valuation evidence cards, and replay status surface through CaseOps. Linked: V5.5. |
|| Storage role: Memory, Analytics, Ledger | Applies | Analytics store: watchlist table and valuation results table. Approved execution ledger: no impact. |
|| Metrics, diagnostics, and operations | Applies | Valuation queue depth, model selection distribution via `tkmetr`; tile health via `tkdiag`. |
|| Security and fail-closed behavior | Applies | Invalid valuation request rejected by capability checks; missing data blocks valuation. |

## Story Breakdown

Stories under V5.5:

- [ ] V5.5.S1: Core capability — watchlist management: CRUD for watchlist tickers with optional notes, persisted to the analytics store, validated (ticker format, duplicate prevention). - TBD
- [ ] V5.5.S2: Automatic valuation queue — watchlist tickers enter V4.32 DCF valuation queue with capability checks (`valuation_market_data.read`, `valuation_financials.read`, `valuation_analysis.analyze`), produce valuation records with intrinsic value range and Monte Carlo CI. - TBD
- [ ] V5.5.S3: Read / query / operator visibility surface — watchlist entries, valuation evidence cards, and replay status visible through CaseOps with full capability scope and policy decision summaries. - TBD
- [ ] V5.5.S4: Evidence, demo, and release closure — T10 expansion covering fixtures, quality gates, documentation, and roadmap updates. - TBD

### How Many Stories

V5.5 is a mid-range epic (watchlist CRUD + valuation queue + visibility + closure): 4 stories.

### Decomposition Rules

1. **Independent delivery**: each story produces an operator-visible outcome
   without waiting for every other story.
2. **One capability per story**: watchlist CRUD in S1, valuation queue in S2 —
   these are distinct capabilities (ticker management vs valuation execution),
   each independently testable.
3. **Split by capability lifecycle**:
   - S1: Core capability — watchlist management: CRUD for tickers with notes,
     validation, stored in the analytics store.
   - S2: Core capability — automatic valuation queue: watchlist tickers enter
     DCF valuation with capability checks, produce valuation records.
   - S3: Read / query / visibility — the operator-facing surface (CaseOps) that
     surfaces watchlist entries, valuation evidence cards, and replay status.
   - S4: Closure story — evidence, demo, documentation, and quality gates via
     child tasks (T10), not a standalone epic-level story.
4. **Closure story**: S4 handles evidence, demo, documentation, and quality
   gates via child tasks (T10) per the story template convention, not as a
   separate story line.
5. **Domain boundaries**: watchlist CRUD (S1), valuation queue (S2), and CaseOps
   surface (S3) are separate stories, not features within one story.

### Bad Splits (Avoid)

- Watchlist CRUD + "ticker validation" as two stories. Validation is part of
  the CRUD contract.
- "Backend" + "frontend" as two stories. Both should be independently testable
  outcomes.
- "Database" + "API" as two stories. Same reason.
- Anything that requires ALL other stories to complete before one acceptance
  criterion can be tested. That is not independent delivery.

### Story Line Format

List stories in delivery sequence. Each line has:
- Story ID (V5.5.SN)
- Self-contained title describing the deliverable
- Link to the GitHub issue (TBD at epic creation)

## Epic Acceptance

- [ ] All required story issues are done or explicitly deferred with rationale.
- [ ] The demo moment succeeds using deterministic local inputs.
- [ ] Relevant policy, audit, replay, adapter, API/UI, metrics, diagnostics,
      and documentation artifacts are linked.
- [ ] Non-goals and deferred work are visible in the roadmap.
- [ ] No live model, broker, payment, trading, crypto, ledger, or execution
      side effects occur unless this epic explicitly enables an approved sandbox
      path.

## Release / Evidence Gate

- What can the user or operator do now that they could not before this epic?
  Maintain a watchlist that *actively values* every ticker you add — automatic
  DCF intrinsic value range, Monte Carlo CI, complete audit trail, fully
  replayable. It's not a list. It's an analysis engine that runs on your
  behalf.
- What changed from the previous roadmap increment?
  M4 delivered replay discipline; V5.26–V5.4 deliver portfolio, exposure, and
  rebalance. V5.5 closes the loop by adding watchlist-driven valuation.
  Before V5.5, you could manage a portfolio but not explore outside it.
  After V5.5, every company on your watchlist gets institutional-grade
  analysis — automatically.
- What is this epic's wow-effect?
  Add a ticker to the watchlist and watch it get fully valued — DCF
  intrinsic value range, Monte Carlo CI, complete audit trail, replayable.
  The wow isn't that it works. The wow is that it was *instant* and *free*
  and *replayable from scratch*. That combination doesn't exist in retail
  investing.
- How does this epic progress Tickoni's unique value proposition from
  doc/strategy/positioning.md?
  Valuation is policy-gated, auditable, and replayable — not a black-box
  API call but a governed financial event. This turns what every other
  platform offers as a static chart into a forensic, reconstructible process.
  The comparison shifts from "better charts" to "better trust."
- Which demo command or CaseOps flow closes the epic?
  Deterministic demo: add watchlist ticker -> enter valuation queue ->
  complete valuation -> replay with substitution.
- Which account, beneficiary, IBAN, wallet, rail, currency, market, venue, asset
  class, instrument, notional, amount, exposure, and frequency checks are
  enforced, if any?
  Ticker format validation; capability checks (`valuation_market_data.read`,
  `valuation_financials.read`, `valuation_analysis.analyze`) on every valuation
  queue entry. No trading execution.
- What happens when the requested action exceeds policy, scope, or evidence?
  Missing market data or financials blocks valuation; malformed valuation
  request rejected by capability checks.
- Is execution paper-only, draft-only, sandbox, live, or disabled?
  Disabled — analysis-only, no execution path.
- Which fixtures, samples, audit records, replay capsules, screenshots, or API
  examples prove the behavior?
  Deterministic demo: add watchlist ticker (AMZN) -> enter valuation queue with
  capability checks -> complete valuation record -> replay capsule substituting
  model and adapter outputs.
- Can replay run without external side effects when replay applies?
  Yes — replay substitutes captured model and adapter outputs; no external calls.
- What intentional blocked-flow or divergence example proves fail-closed behavior?
  Missing market data or financials blocks valuation; malformed valuation
  request rejected by capability checks.

## Dependencies And Decisions

- V4.32 DCF valuation engine must be available; V5.5 consumes the
  valuation output format defined in V4.22.S18.
- `tkdisp` schedules queued valuations as bounded agent runs vs a dedicated
  `tkvalq` tile. **Decision: `tkdisp` keeps governance consistent.**
- Decision: Valuation queue ownership — `tkdisp` schedules queued valuations
  as bounded agent runs. **Decision: `tkdisp` keeps governance consistent.**
